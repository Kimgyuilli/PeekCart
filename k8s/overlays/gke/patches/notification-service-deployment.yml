# GKE: e2-standard-4 기준 resources 상향. strategic merge — container name(notification-service) 매칭.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: peekcart
spec:
  template:
    spec:
      containers:
        - name: notification-service
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 2000m
              memory: 2Gi
