# audit — task-d026-d002a-read-ceiling-session

## 2026-09-20 — 계획 리뷰 라운드 1
- 상태: 수행(1라운드)
- 항목: 13건 (P0:0, P1:10, P2:3)
- 처리: **반영 11건 / 절반 기각 2건 / 이월 0건 / 전면 기각 0건**
- 절반 기각 2건 (근거 있는 축소):
  - **#2 키 분포 대조 run 추가** → 기각. k6 키 순회(`__ITER % IDS`)는 **기준선과 같아야 하는
    조건**이다(N2). 바꾸면 비교 가능성이 깨져 얻는 것보다 잃는 게 크다. 나머지 절반(`20회/초`를
    전제에서 측정 대상으로 내림)은 반영. 부하 모델 개선은 §5 로 이월 — 별도 세션에서 기준선을
    새로 잡는 방식이 맞다.
  - **#7 Prometheus 스택 추가** → 기각. overlay 가 얇은 것이 **의도**이고 측정 대상 시스템을
    바꾸면 안 된다. 나머지 절반(CPU 수집 경로를 cgroup `cpu.stat` `throttled_usec` 차분으로
    고정 + k6 connect/blocked/failed 기록)은 반영.
- 뒤집힌 전제 3건 (전부 직접 확인 후 수용):
  - **#11 — §V6 서술 오류.** 초안 "`CacheConfig` 전체가 `@ConditionalOnProperty` 하위" 는 틀렸다.
    클래스 선언은 `@Configuration`+`@EnableCaching` 뿐(`:49-51`)이고 실제로는 `cacheManager`(`:90-92`)
    ↔ `noOpCacheManager`(`:138-141`) **상호배타 두 빈**이다. **결론(단일 스위치)은 유지**, 근거만 정정.
  - **#9 — 명명 오류.** 초안이 "D-002 P5" 라 부른 것은 `task-d002-bc-session.md:85` 의 P5 이고
    **주문 경로에서 이미 수행**됐다(`d002bc-gke-20260917.md:36-46`, ×2.94). `TASKS.md:67` 도 D-002 를
    완료로 적는다. 완료된 행을 되살리지 않도록 **`D-002a 읽기 경로 잔여`** 로 개명하고
    계획서 파일명·브랜치명도 함께 바꿨다.
  - **#1 — N6 신설.** `mysql.yml:24-34` 가 `Recreate`(D-023) 라 CPU 전환이 버퍼풀 콜드 리스타트를
    동반한다. 단방향 관측은 CPU 효과를 증명하지 못한다 → **A→B→A′ 되돌림 블록**과 되돌림 게이트 신설.
- 새로 확인된 사실 3건: **V13** `gke-d002a` 에 StorageClass patch 부재(`gke-d002bc` 엔 있음) ·
  **V14** `loadtest/cleanup.sh` 가 이미 존재하고 D-002a 의 잔존 PVC 3개 사고 뒤 exit code 검증까지
  들어가 있음(새로 쓰지 않고 재사용) · **V15** Redis 가 PVC 를 써서 재배포로 초기화가 보장되지 않음.
- 계획 변경 규모: 명제 9 → **11** · 작업 항목 P1~P8 → **P0~P8** · 측정 8런 → **30런**(A→B→A′ · 셀당 3회).
- raw: `.cache/codex-reviews/plan-task-d026-d002a-read-ceiling-session-*.json` (파일명은 개명 전
  `plan-task-d026-d002p5-gke-session-1789835536.json`)

## 2026-09-20 — 계획 리뷰 라운드 2
- 상태: 의도적 생략(plan: frontmatter `codex: off` — 사용자 지시 "코덱스 리뷰 돌리지 마세요",
  **이번 세션 한정**)
- 경위 정정: 최초에 `.cache/codex-off`(지울 때까지 전역)로 적었으나 사용자가 범위를 이번 세션으로
  한정했다 → 파일을 지우고 계획서 frontmatter 로 옮겼다. 다른 task 에는 걸리지 않는다.
- **절차 결함 1건 — 이 세션에서 하네스에 반영했다.** 라운드 1은 `hpx_codex_allowed` 게이트가
  `allowed` 를 낸 것만 근거로 호출했다. 확인 결과 `plan.md`·`work.md` 어디에도 "호출 전 사용자
  승인" 이 없었고(승인을 요구하는 자리는 task 확정·ADR 선행·재리뷰 3회 초과 셋뿐) 게이트의
  "판정을 받는다" 는 스크립트 판정을 가리킨다. 그러나 호출은 **사용자 쿼터를 쓴다** — D-027 ② 가
  실측한 빈 응답 44/282 중 사용량 한도가 9건이었다. `plan.md` §5-0 · `work.md` §5-0 에
  **승인 게이트를 2단계로 명문화**했다.

## 2026-09-20 — diff 리뷰 (/work)
- 상태: **의도적 생략**(plan: frontmatter `codex: off` — 사용자 지시, 이번 세션 한정)
  - 게이트 4행 그대로: `blocked` / `plan` / `frontmatter codex: off` /
    `docs/plans/task-d026-d002a-read-ceiling-session.md`
  - 정상 종결이다. 미충족 항목이 아니다(§9). 단 **`P0/P1 = 0` 을 주장하지 않는다.**
- 진행: **P0·P1 완료**, P2 진행 중, P3~P8 미착수(P3 이 과금 경계)
- **선재 결함 1건 (범위 밖, 손대지 않음)**: `k8s/overlays/gke-d002a` 는 `../../base/*.yml` 을
  파일로 직접 참조해 kustomize root 이탈 제한에 걸린다 → **기본 설정으로 렌더 불가**.
  HEAD 상태에서도 동일 실패를 확인해 **내 변경이 만든 것이 아님**을 확정했다. 구조 수정은
  이 세션 범위 밖이라 `--load-restrictor LoadRestrictionsNone` 을 정본 렌더 명령으로 명시하고
  overlay 주석에 사유를 남겼다. 승격 여부는 `/ship` 에서 판단.
- **계획 대비 구현 차이 1건 (계획서를 먼저 고쳤다)**: P1 은 "MySQL 조건을 명명된
  overlay(또는 component) 로 선언" 이었고, 구현은 **sibling overlay 2개**
  (`gke-d002a-mysql-500m` / `-2000m`)로 갔다. component 방식은 소비자 overlay 가 결국
  하나여서 "두 조건이 각각 렌더되는 이름을 갖는다" 는 N5 의 목적을 충족하지 못한다.
- 검증(P1): 두 overlay 렌더 성공 · mysql cpu **500m / 2000m** · product cpu **3000m**(양쪽) ·
  PVC 3개 `standard-rwo`. **실패 주입 통과** — patch 제거 시 base 500m 복귀, 복원 시 2000m.
- compound capture: 3-질문 적용 결과 **prose 저장 안 함**. "측정 조건을 overlay 에 선언하지 않으면
  잊는다" 는 ① 반복되나 ② 재도출 쉽고(렌더하면 바로 보임) ③ **더 싼 자동검사로 바꿀 수 있다** —
  측정 overlay 를 `kustomize-namespace-lint.sh` 의 `OVERLAYS` 에 넣어 렌더를 강제하는 쪽이
  맞다. 단 위 선재 결함(root 이탈) 때문에 지금 넣으면 즉시 red 다 → **/ship 에서 부채로 제안**.

## 2026-09-20 — 측정 세션 실행 (P0~P8)
- 30런 전부 유효(블록 A 12 · B 12 · A′ 6, 셀당 3회). 증적
  `docs/progress/evidence/d026-d002a-read-ceiling-20260920.md`.
- **명제 판정**: N1 ✅ · N2 ✅(재현 게이트 2개 통과) · N3 ✅(4자 대조 완결 — push digest =
  렌더 image = Pod imageID = git SHA `b0d8fd9`) · N4 **실패→해석함**(§읽는 법 ①) ·
  N5 ✅ · N6 **detail ✅ / list 실패** · N7 — · N8 ✅ · N9 ✅(차분) · N10 → P8 · N11 ✅
- **게이트 5개 중 2개 실패. 통과로 적지 않았다.** 대조군(+16.7%)은 원인을 분리해 해석했고
  (바닥 DB 바운드 고정 ↔ 천장 앱 바운드 이동), 되돌림 list(+11.5%)는 **숫자를 확정하지 않고**
  방향만 적었다.
- **헤드라인을 바꿨다**: `×1.23→×2.30`(세션 간, 오염) 대신 **paired ratio 0.60→1.01**(세션 내).
  계획서 §3-3 이 이 판정 방식을 미리 정해둔 것이 결정적이었다.
- **블록 B 가 블록 A 의 해석을 정정했다** — A 의 1.01 은 동률이 아니라 둘 다 ≈900 rps 천장에
  눌린 착시였고, 천장을 풀면 detail 은 list 의 81%. **되돌림 블록(리뷰 지적 #1)이 없었으면
  "동률 달성"으로 잘못 적었을 것이다.** 라운드 1 지적 중 가장 값이 컸다.
- **내 하네스 결함 4건**(실행 중 스크립트 편집 · rollout 직후 Pod 선택 경합 · CPU 단위 비교 ·
  캐시 3개 중 2개만 분해). ②는 대조군 3런을 통째로 먹고 있었다. 전부 수정·재실행.
- **예측 대비**: 계획서 §V10 이 우려한 400 VU TTL stampede **미관측**(miss 10/s, 이론 20/s 의
  절반). 예측을 먼저 적어둔 덕에 "어긋남" 을 발견으로 기록할 수 있었다.
- compound capture: 3-질문 적용 → **prose 저장 안 함.** "실행 중 스크립트를 편집하지 말 것" 은
  ③ 더 싼 자동검사로 대체 가능(런너를 실행 전 복사해 쓰면 구조적으로 불가능) → 자동화로 라우팅.
