# audit — task-gke-measurement-closeout

## 2026-09-22 03:29 — 계획 리뷰 라운드 1
- 상태: 수행(1라운드)
- 항목: 9건 (P0:0, P1:8, P2:1)
- 처리: 반영 8건 / **이월 1건** / 기각 0건
- 이월 항목: **[7] jitter 의 DLQ 감소 효과 실증** — `ProductKafkaConfig:118` 에
  `new JitteredSequenceBackOff(...)` 가 하드코딩돼 토글이 없다(V9). 동시 A/B 가 불가능하므로
  이번 세션은 **기준선 산출까지만** 닫고 효과 주장은 하지 않는다. 계획서 §1 명제 2 와 §5
  완료 조건을 그만큼 **축소**했고 §6 에 재검토 조건(측정용 토글 신설 / 두 빌드 교대 예산)을
  적었다. 국소적이라 부채 행으로 승격하지 않는다.
- 뒤집힌 전제:
  - **V2 부분 반증** — 초안은 "조건 B 를 원 세션과 동일하게 재현한다" 고 적었으나, 증적
    `d002a-gke-20260916-0030.md` 는 CPU·VU 만 기록하고 **IDS·DUR·워밍업·집계 구간은 어디에도
    없다**(증적·계획서·README 전수 grep 0건, 직접 확인). → 이번 세션이 그 값을 **신설·고정**하고,
    ×1.23 과의 비교는 **list 대조군이 ×2.02 ± 15% 안에 들 때만** 직접 비교로 취급한다.
  - **V8 추가 반증** — 초안은 `pending.tasks` 를 "event-loop lag 직접 지표" 로 적었다.
    reactor-netty 1.2.16 바이트코드 확인 결과 `EventLoopMeters` 의 상수는 `PENDING_TASKS`
    하나뿐이고 이것은 **큐 깊이이지 시간 단위 lag 이 아니다**. 명제 3 과 P12 의 표현을 정정하고
    시간 단위 lag 은 §6 으로 옮겼다.
  - **[8] cleanup 완결성** — `loadtest/cleanup.sh:47-115` 가 클러스터·VM·디스크·IP 만 다루고
    **AR 이미지 단계가 없음**을 확인. "AR 태그 삭제" 를 검증 불가능한 완료 조건으로 두고 있었다
    → P16 으로 분리하고 재조회 0건 확인을 검증에 넣었다.
- 그 외 반영: [2] run 격리 절차(rollout 대기·FLUSHALL·워밍업/집계 구간 분리, 자기모순 문장 삭제) ·
  [3] 3회 반복 + 중앙값 + 허용오차 숫자화 · [4] SQL 경로·계정 명시 · [5] series/라벨 확인과 명칭 정정 ·
  [6] 측정 전용 빌드의 브랜치·digest·base 미변경 guard·복귀 순서 · [9] P14 를 P14~P17 로 분리
- 범위 변화: 작업 항목 14개 → **17개**. 늘어난 3개는 전부 cleanup·검증 분리라 실행 부담이
  아니라 판정 가능성의 증가다. 반대로 §1 명제 2 는 **축소**됐다.
- raw: `.cache/codex-reviews/plan-task-gke-measurement-closeout-1790014936.json`
