# task-codex-review-render — audit

## 2026-09-20 — 계획 리뷰
- 상태: 해당 없음(등급 M 으로 시작) — 계획 리뷰는 L 등급만 수행한다.
  이후 구현 중 P6 편입으로 **등급이 L 로 올랐으나**, 계획 리뷰 시점은 이미 지났으므로
  소급 수행하지 않는다. diff 리뷰는 L 기준(라운드별 블록)으로 기록한다
- 코드 검증: 수행(§2 표 11행). `.cache/codex-reviews/*.json` 282개 전수 실측
- ADR: 대상 아님 — 새 외부 의존성·경계 변경·인프라 전부 없음

## 2026-09-20 — diff 리뷰 라운드 1
- 상태: 수행(1라운드)
- 항목: 4건 (P0:0, P1:2, P2:2). 전부 `suggested_disposition: reflect`
- 처리: **반영 3.5건 / 이월 0.5건 / 기각 0건**
  - [1] P1 `items` 타입 미검증 → **반영.** `{"items":{}}` 가 exit 0 `지적 0건` 으로 샜다.
    직접 재현 확인. 이 작업의 명제를 정면으로 위반하는 경로라 최우선 처리
  - [2] P1 `--round` 값 누락 시 무한 루프 → **반영.** 3초 후에도 프로세스가 살아 있음을
    직접 재현. `shift 2` 실패가 루프를 전진시키지 못한다. 인자 검증 전반을 함께 정리
  - [3] P2 `refute()` 가 종료 코드를 안 본다 → **부분 반영.** 종료 코드 검사는 넣었다.
    "표 헤더를 파싱해 컬럼 계약을 검사하라" 는 후반부는 **이월**
  - [4] P2 P6 반영 후 낡은 서술 잔존 → **반영.** 렌더러 각주·헤더 주석·audit 을 최종 상태로
    정정. 내가 만든 불일치다
- 이월 항목: [3] 후반부 — 표 검증을 문자열 매칭에서 헤더 파싱으로. 재검토 조건: 컬럼 구성이
  한 번 더 바뀔 때. 지금은 V7/V8 이 `check()` 와 짝지어 있어 단독 false-green 은 아니다.
  1건이고 국소적이라 부채 행으로 승격하지 않고 PR 본문 §이월 에만 남긴다
- 라운드1 수정이 만든 새 결함: V15 가 `check()` 에 인자 전달을 끼워넣으려다 실패 → 전용
  `argcase()` 로 분리해 해소. selftest 내부 문제이고 렌더러에는 영향 없었다
- 재리뷰: **하지 않는다.** §7 조건은 "라운드 1에서 P0 를 실제로 수정했다면" 인데 P0 는 0건이다
- 뒤집힌 전제: 스키마 `required` 이월 판단(§7 정정 이력 참조). 현재 API 가 기존 스키마를
  400 으로 거부해 Codex 리뷰가 전면 중단 상태였고, 그 사실을 이 PR 의 리뷰 시도에서 렌더러가
  잡아냈다. 사용자 확인 후 P6 으로 편입하고 등급을 L 로 올렸다
- 검증: `./gradlew test` BUILD SUCCESSFUL (제품 코드 무변경) · selftest V1~V15 **35건 전부 통과**
- raw: .cache/codex-reviews/diff-task-codex-review-render-r1-*.json
