# PeekCart — Task 관리

> 현행 작업을 **PR 단위**로 추적한다. PR 1개 = 부채/기능 1묶음.
> 상태: `🔲 대기` / `🔄 진행 중` / `✅ 완료` / `⏸ 보류`

## 문서 맵

| 대상 | 경로 |
|---|---|
| Phase 1~3 task 이력 (아카이브) | `docs/progress/TASKS-archive-phase1-3.md` |
| Phase 4 설계·실행 로드맵 (ADR 시퀀싱·구현 순서) | `docs/progress/phase4-design-roadmap.md` |
| 진입 전 부채 해소 로드맵 (버킷 1 완결, 버킷 2/3 이관·게이트) | `docs/progress/phase4-prep-debt-roadmap.md` |
| 부채 후보 분류·승격 매핑 (L-001~L-022) | `docs/progress/phase4-prep-debt-roadmap.md §2~5` |
| Phase별 작업 이력 | `docs/progress/PHASE1.md` · `PHASE2.md` · `PHASE3.md` |

---

## 현재 단계: Phase 4 — MSA 분리

> 서비스 경계 정본 = §5(5개 풀 분해) 확정. **초기 설계 ADR(A1~A4) 완료 (#44~#47) → 구현 단계.** 구현 ① PR2 착수 중 전환기 인증 보정 **ADR-0014(A4.5)** 추가(`peekcart-common-auth`, ADR-0011 부분 무효화). 상세: `docs/progress/phase4-design-roadmap.md`.
> 착수 시 상태를 `🔄`, 머지 시 `✅` 로 갱신하고 PR/ADR 링크를 단다. 구현 각 항목의 세부 PR 분할은 해당 항목 `/plan` 착수 시 정의한다.

### 설계 (A1~A4 ✅ 완료 · A4.5 보정 ADR)

| 순서 | ADR | 작업 | 편입 부채 | 상태 |
|---|---|---|---|---|
| A1 | ADR-0010 | 서비스 분해 — §5 비준 + §4-5 정정, 5개 서비스 계약 명문화 (+ F1/F2/F3 정합) | — | ✅ [#44](https://github.com/Kimgyuilli/PeakCart/pull/44) |
| A2 | ADR-0011 | 멀티모듈 구조 (`common`+관측성+5서비스, 의존 규칙·빌드/테스트/이미지 계약) | L-016a, D-016 | ✅ [#45](https://github.com/Kimgyuilli/PeakCart/pull/45) |
| A3 | ADR-0012 | DB-per-service + 이벤트/Saga 계약 (재고 예약·`stock.reservation.result`·retention) | L-008/011, L-020-2 | ✅ [#46](https://github.com/Kimgyuilli/PeakCart/pull/46) |
| A4 | ADR-0013 | Gateway 보안 (RS256·Gateway 검증·Rate Limit·Reuse Detection·S9 관측성) | 보안 묶음 L-001/002/003/019 | ✅ [#47](https://github.com/Kimgyuilli/PeakCart/pull/47) |
| A4.5 | ADR-0014 | 전환기 인증 검증 공유 모듈 `peekcart-common-auth` (게이트웨이 이전, ADR-0011 부분 무효화) — 구현 ① PR2 중 발견 | — | ✅ [#50](https://github.com/Kimgyuilli/PeakCart/pull/50) |

### 구현 (ADR 선행 후 PR 단위) ← 현재 focus

> **구현 ① ✅ 완료 (선행 ADR-0011/ADR-0014, [#48~#68])** — PR1 스켈레톤+common → PR2 서비스 분리(5 peel·root 소멸) → PR3 Dockerfile/CI(#66)·k8s(#67)·관측성(#68). 계획서: `docs/plans/task-impl1-gradle-multimodule.md`.
> **구현 ② ✅ 완료 (선행 ADR-0012 D1/D5·ADR-0016, [#69·#71·#72])** — PR1 교차 FK 드롭 → PR2 물리 스키마 분리(1 인스턴스+5 스키마) → PR3 retention/cleanup 스케줄러(D5·L-008/011 종결). 계획서: `docs/plans/task-impl2-db-per-service.md`. **다음 focus = 구현 ③(Spring Cloud Gateway, 선행 ADR-0013).** (인스턴스 물리 분리는 URL 교체로 가역 승격 — 후속.)
> **⚠️ peel 순서 정정 (2026-06-15)**: Product 가 Order 의 동기 빈(`ProductPort`)에 묶여 ① 단독 peel 불가(부팅 실패) — independent 한 **User 를 PR2b 로 먼저** 떼고, Order/Product/Payment 는 ②(DB)/④(Saga)/⑤(캐시)를 교차한 사가 클러스터로 함께 분리한다(ADR-0010 F2·ADR-0012 D3, 새 ADR 불필요). 상세: `phase4-design-roadmap.md §2`.

| 순서 | 작업 | 선행 ADR | 편입 부채 | 상태 |
|---|---|---|---|---|
| ① | Gradle 멀티모듈 전환 (PR1 ✅ [#48](https://github.com/Kimgyuilli/PeakCart/pull/48) · PR2a-1 ✅ [#51](https://github.com/Kimgyuilli/PeakCart/pull/51) common-auth 추출+JWT verify/sign 분리 · PR2a-2a ✅ [#52](https://github.com/Kimgyuilli/PeakCart/pull/52) SlackPort→:common+ADR-0011 §D2 정정 · PR2a-2b ✅ [#53](https://github.com/Kimgyuilli/PeakCart/pull/53) notification-service peel(첫 서비스 분리)+ActuatorSecurityConfig(S4)+공유스키마→:common · PR2b ✅ [#55](https://github.com/Kimgyuilli/PeakCart/pull/55) user-service peel(발급 owner·blacklist token-hash dual-read·SlackNotificationClient @ConditionalOnProperty) · **사가 클러스터 strangler-1 ✅ [#56](https://github.com/Kimgyuilli/PeakCart/pull/56) 재고 예약/복구 이벤트화(예약 원장 상태머신·all-or-nothing·CAS 복구, ADR-0010 F2·ADR-0012 D3)** · **strangler-2 ✅ [#57](https://github.com/Kimgyuilli/PeakCart/pull/57) 단가 로컬 캐시 CQRS(product.updated 발행·@Version 순서키·원자 upsert stale-skip·getUnitPrice 동기 seam 제거, ADR-0012 ⑤·L-006)** · **strangler-3 ✅ [#58](https://github.com/Kimgyuilli/PeakCart/pull/58) 2-phase 예약 확정(CONFIRMED)/보상 + 결제 게이트(ORD-008·paymentRequestedAt 타임아웃)(ADR-0012 D3/④)** · **strangler-4 ✅ [#61](https://github.com/Kimgyuilli/PeakCart/pull/61) `verifyProductExists` 로컬 캐시화(ProductPort 동기 seam 제거·캐시 미스 ORD-009·order↔product src 결합 금지 가드)(ADR-0010 F2·ADR-0012 ⑤) → Order↔Product production 동기 결합 0** · **Product peel ✅ [#62](https://github.com/Kimgyuilli/PeakCart/pull/62) product-service 모듈 분리(첫 *발행* 서비스: outbox/idempotency/ShedLock 복제·공유 DB poller 소유권 분리 aggregateType allowlist·ProductSecurityConfig)+root 테스트 디커플(ADR-0010/0011/0012/0014, DB 물리분리는 ② 이연)** · **strangler-5 Order↔Payment 동기 결합 제거 ✅ [#63](https://github.com/Kimgyuilli/PeakCart/pull/63) `OrderPort` seam 제거(verifyOrderOwner→payment-로컬 userId·transitionToPaymentRequested→`payment.requested` 이벤트)+reserve→pay/취소 게이트 payment-로컬 복원·@Version·이벤트 역전 영속 marker(ADR-0012 §D4 refine)+가드 `assertNoOrderPaymentSourceCoupling` → Order↔Payment src 동기 결합 0** · **Order peel PR-a ✅ [#64](https://github.com/Kimgyuilli/PeakCart/pull/64) order-service 모듈 분리(order 도메인+16테스트 이관·outbox/idempotency/ShedLock 복제·OrderKafkaConfig producer-owns NewTopic(order.*)·OrderSecurityConfig·data-redis 무조건(ADR-0014 blacklist)·root poller PAYMENT 좁힘·root 통합테스트 payment-observable 디커플·OrderApplicationTests 부팅 스모크, ADR-0010/0011/0012/0014)** · **Payment peel + root 해체 PR-b ✅ [#65](https://github.com/Kimgyuilli/PeekCart/pull/65) payment-service 모듈 분리(마지막 도메인)+root app 해체(global/app/src 삭제·bootJar→aggregator)+order-service Flyway migrator 승계(B5)+product-service NewTopic 소유+global 테스트 rehome(:common 유닛·payment-service 통합)+root 단일 이미지 제거(PR3 이연), ADR-0010/0011/0012/0014) → 5개 서비스 풀 분해 완료(root app 소멸)** · **PR3a ✅ [#66](https://github.com/Kimgyuilli/PeakCart/pull/66) 서비스별 Dockerfile(단일+ARG SERVICE·멀티모듈 COPY·base digest 고정 L-016a)+CI 이미지 매트릭스(images build/smoke·publish main push job 분리·save/load·digest 강제)+image-contract-lint per-service(canonical 5·images/publish matrix 일치·전환기 SUSPENDED 게이트)+smoke 공유스키마 선행 마이그레이션(flyway 이미지). 후속부채: flywayMigrateShared 깨짐(Docker flyway 정본 우회)** · **PR3b ✅ [#67](https://github.com/Kimgyuilli/PeakCart/pull/67) k8s base/overlays per-service 재구성(5서비스 deployment/cm/secret/servicemonitor·비-order initContainer order-service readiness gate·minikube/gke overlay patch×10·gke images[]5·order-service 단일 HPA)+Slack real↔no-op 게이팅(SlackFallbackConfig·notification fail-fast/product·order·payment no-op·presence-based+placeholder 함정 제거)+자격증명 fail-fast(SLACK/TOSS committed secret 제거·smoke 런타임 주입)+mysql-secret 분리(infra→secret dangling)+servicemonitor count==5·image-contract full 5/5·promote-images(D-016)(ADR-0004/0005/0006/0007/0010/0014). 후속: PR3c 관측성+ADR-0015·full lint-digest** · **PR3c ✅ [#68](https://github.com/Kimgyuilli/PeakCart/pull/68) 관측성 per-service 재설계(grafana alert 8 rule by-clause+regex·scrape-absent 5 equality·dashboard $application custom 변수·observability lint 2종 per-service 재작성+CI 재활성+promtool syntax+sweep 가드)+ADR-0015 신규·ADR-0009 Partially Superseded(ADR-0006/0009/0010/0015). negative test 6종 false-green 차단** → **구현 ① PR3 전체 종료(이미지/CI #66·k8s #67·관측성 #68)**) | A2·A4.5 | L-016a, D-016 | ✅ |
| ② | 서비스별 DB 분리 (PR1 ✅ [#69](https://github.com/Kimgyuilli/PeakCart/pull/69) 교차 도메인 FK 6개 드롭(`fk_carts_user`·`fk_cart_items_product`·`fk_orders_user`·`fk_order_items_product`·`fk_payments_order`·`fk_notifications_user`, 컬럼 유지·ID 참조 대체)·소유 경계 검증(5서비스 자기 테이블만 매핑)·8모듈 그린, ADR-0012 D1 — 물리 분리 선행 저위험 단계 · 계획서 `task-impl2-db-per-service.md` PR1/PR2/PR3 분할) · **PR2 ✅ [#71](https://github.com/Kimgyuilli/PeakCart/pull/71)** Flyway per-service 통합 베이스라인(5× `V1__init_<svc>.sql`·`:common/db/migration` 소멸·flywayMigrateShared 제거)+물리 스키마 분리(1 인스턴스+5 스키마 `peekcart_<svc>`+계정/격리 GRANT)+datasource per-schema(ADR-0007)+outbox allowlist 제거(스키마 분리로 자연 소유권·B8b)+k8s mysql init ConfigMap(.sh·비밀번호 Secret env·literal 금지)·per-svc secret 분화·initContainer 게이트 제거+compose/smoke mysql init 전환+통합테스트 cross-domain 시드 제거·cleanDatabase 스키마 적응형+**ADR-0016 신규**·ADR-0012 Partially Superseded·Layer1 동기화(8모듈 그린, GRANT 최소권한 DROP 미부여) · **PR3 ✅ [#72](https://github.com/Kimgyuilli/PeakCart/pull/72)** retention/cleanup 스케줄러(processed_events·outbox_events, ADR-0012 D5) — floor cross-field fail-fast(`IdempotencyRetentionProperties` @AssertTrue max(4창)≤retention·소유 서비스만 @EnableConfigurationProperties→user 누출 0)+서비스×잡 매트릭스(processed=product/order/payment/notification·outbox=product/order/payment·user=0, 물리 배치)+배치 삭제 계약(cutoff 1회·batch-size×max-batches-per-run·per-batch @Transactional·V2 삭제기준 인덱스)+outbox predicate `PUBLISHED AND published_at<cutoff`(PENDING/FAILED/NULL 보존)+notification 신규 ShedLock 인프라(dep·ShedLockConfig·@EnableScheduling·shedlock 테이블)+정책키 base-only(ADR-0007)+5서비스 매트릭스 통합테스트(8모듈 그린) → **L-008/011 종결** | A3 | L-008/011 | ✅ |
| ③ | Spring Cloud Gateway (PR1 ✅ [#73](https://github.com/Kimgyuilli/PeakCart/pull/73) RS256/JWKS dual-validation — `JwtKeyProperties`/`PemKeyLoader`/`RsaPublicKeyRegistry`·verifier alg allow-list(kid 선택·HS512 fallback bounded off)·User RS256 서명(kid)+JWKS `/.well-known/jwks.json`·개인키 산출물 비포함(testFixtures/파일마운트)·latency 측정, ADR-0013 D1/D2) · **PR2 ✅ [#74](https://github.com/Kimgyuilli/PeakCart/pull/74) Refresh Token Reuse Detection** — 삭제 기반→`family_id`/`status`(ACTIVE/ROTATED/REVOKED) 상태전이(V2·`token_hash` sha256·평문 미저장)+원자 조건부 UPDATE(`rotateActive`/`consumeGraceOnce`/`forceRotate`/`revokeFamily`, Redis GETDEL grace 대체)+`AuthService.refresh` 재작성(grace 1회·reuse 진입 3경로 `detectReuse` 단일화)+reuse 무효화 커밋 보존(`RefreshTokenReuseException`+`@Transactional(noRollbackFor)`, REQUIRES_NEW self-deadlock 폐기)+`family_id` claim(`TokenIssuer`/`TokenClaims`)+Redis `auth:deny:family:<id>` write/read+전환기 common-auth `JwtFilter` enforcement(family_id 부재 레거시 blacklist-only·Redis deny 실패 격리)(ADR-0013 D4) · **PR3a ✅ [#75](https://github.com/Kimgyuilli/PeakCart/pull/75) Gateway shadow 배포** — `gateway` 모듈 신설(WebFlux·`:common` 미의존 — common 이 servlet starter-web 을 `api` 전이 노출해 MVC 부팅 유발, 가드 `assertGatewayHasNoServletDeps`+REACTIVE 부팅 테스트 이중 고정)+라우트 정본 확정(`cart` 단수·`admin/products` 선순위·JWKS/swagger 외부 미노출)+reactive 인증 필터(외부 `X-User-*` 항상 strip→RS256(JWKS)/전환기 HS512 검증→blacklist·family deny→신뢰 헤더 주입, **응답 계약 401/429/503 분리**)+JWKS **snapshot 통째 교체**(폐기 kid 즉시 무효화·실패 시에만 LKG)+**fail-closed RateLimiter 자체 구현**(SCG 기본은 Redis 오류를 `allowed=true` 로 삼켜 fail-OPEN → ADR-0013 D3 위반)+readiness/liveness 분리(cold start 트래픽 미수신)+actuator 관리 포트 8081 분리+Dockerfile/CI matrix 6·canonical **도메인 5+인프라 1** 분리(gateway k8s 매니페스트는 PR3b → `IMAGE_CONTRACT_TRANSITION=1` 전환기 게이트 재설정) · **PR3b ✅ [#76](https://github.com/Kimgyuilli/PeakCart/pull/76) gateway k8s 배포 표면** — base 매니페스트(Deployment+Service+ConfigMap·`k8s/base/services/gateway/`)+overlay(minikube NodePort **30080** 승계·gke Internal LB·patch×4·`images[]` 6)+**gateway HPA**(minReplicas 2 — order-service 단일 HPA 원칙의 명시적 예외, SPOF 근거 ADR-0013 D3)+`application-k8s.yml` 신설(k8s 연결값 단독 소유·라우트 placeholder `${app.gateway.upstream.<svc>-uri}` 정규화, ADR-0007)+**`IMAGE_CONTRACT_TRANSITION` 제거 → image-contract-lint full 6/6**(PR3a 꼬리 종결)+**`gateway-exposure-lint` 신설**(렌더가 통과시키는 노출 위반 전담 — Service 8080 단일·`targetPort` 고정·Job/CronJob/Pod 포함 PodSpec 전수·Secret 참조 전무·hostNetwork/hostPort·configMap 배선·`maxUnavailable=0`, self-test **13종 진단 대조**)+**`observability-promql-lint` 정정**(S6.d 기대집합을 전체 Service glob→**SM 매칭 Service** 기준으로, ADR 변경 없음)+롤아웃 runbook(계획 §7)**결정: ServiceMonitor·Secret 미생성**(SM 은 ADR-0015 S5/S6.d 계약 변경 동반이라 PR4 이연 / gateway 소비 비밀 0). **canary 실증적 미확보 — PR3c GKE 보안 smoke 세션 합류** · **PR3c ✅ [#77](https://github.com/Kimgyuilli/PeakCart/pull/77) header-trust 전환 + 5서비스 ClusterIP 환원 + NetworkPolicy** — `HeaderAuthenticationFilter`(3-state: 부재→anonymous/정상→인증/형식오류→401 entrypoint)+`HeaderTrustSecurityConfigurer`(JwtSecurityConfigurer 공통 정책 전부 보존·필터만 교체, 구 servlet verifier 잔존)+5 SecurityConfig 전환+`LoginUser(userId,role,familyId)`·`logout(userId,familyId)`(family deny+revokeAllByUserId, family-less=deny 생략)+overlay service patch 10개 삭제→ClusterIP 환원·base `maxUnavailable:0`+**NetworkPolicy**(ingress-only·podSelector `component:backend`·gateway peer+monitoring scrape 예외, SA 미도입 — vanilla NP 는 SA peer 미지원)+**`networkpolicy-contract-lint`**(고정 5 Deployment 이름 식별·peer TCP8080 결합, self-test 8종)+**`gke-security-smoke`**(barrier enforcement hard-fail[Dataplane V2 OR networkPolicy.enabled]+non-gateway 차단+직접경로 도달불가+canary). **Codex diff 리뷰 6건(P1×4 barrier/lint false-green+P2×2) 전량 반영**. **✅ GKE 실 클러스터 smoke 증적 확보(2026-08-08, [#79](https://github.com/Kimgyuilli/PeakCart/pull/79))** — `gke-security-smoke.sh` barrier 5/5(enforcement `ADVANCED_DATAPATH`·non-gateway 차단·공개 200·scrape up=5·직접경로 5개 도달불가)+canary 3/3(공개 200/보호 401/spoof 401). **검사(5) 는 3상태 양성 대조군으로 vacuous-green 제거**(LB有·NP無=200 → LB有·NP有=000 → ClusterIP·NP有=000: NP 단독 효과와 표면 제거를 분리). 증적 `docs/progress/evidence/pr3c-gke-smoke-20260808-1445.md`. **잔여 편차**: user-service 개인키를 ADR-0013 D2 의 Secret Manager+CSI 가 아닌 k8s Secret 으로 마운트 → **PR3d P5 CSI 계약은 본 증적으로 미충족** · **PR3d 재정의(ADR-0017 Accepted, 경로 A)**: 평문 header-trust → **Gateway 서명 내부 토큰(`X-Internal-Auth`) 격상**(defense-in-depth — NetworkPolicy AND 서명) + Authorization 전달 중단 + 서비스 측 사용자 토큰 verifier·blacklist 삭제(ADR-0014 D2-c exit, `RsaPublicKeyRegistry`/`PemKeyLoader` 는 삭제 아니라 내부 토큰 검증기로 **용도 변경**). 정본 `docs/plans/task-impl3-pr3d-internal-token.md`(P1~P10). **§9 에서 PR3d-a(코드) / PR3d-b(키배포·클러스터)로 분할 확정** — 분할 기준 = "클러스터 없이 그린이 되는가", 근거 §9.1 코드 검증 3건(JwtFilter 는 PR3c 이후 이미 미배선 → loop2 #1 위험 소멸·rollback 행렬 전제 소멸 / user-service 개인키 k8s 매니페스트 부재 → P7 key-ownership lint 가 vacuous-green 이라 b 가 user 키도 정본화). · **PR3d-a ✅ [#80](https://github.com/Kimgyuilli/PeakCart/pull/80)** 평문 header-trust 폐기 → Gateway 서명 내부 토큰(P1·P2·P3·P4·P6·P9+P7 property-ownership): `internal-token-contract` 신설(10모듈, 프레임워크 의존 0 — 발행 gateway/검증 common-auth 가 서로 의존 불가라 이름 계약 단일 출처, 루트 가드 allowlist 1건+계약모듈 무오염 (a2) 검사)·`InternalTokenIssuer`(개인키 부팅 fail-fast·family-less 발행거부 401)+`GatewayAuthenticationFilter` 재작성(평문 3종→서명 토큰 단일 주입·strip 확대·**Authorization 전달 중단**)·`InternalTokenVerifier`+**전용** `InternalGatewayPublicKeyRegistry`(alg RS256 정확 핀·iss/kid/exp/iat·수명상한·skew·claim 타입 전부 검증, **키 도메인 분리** — User JWKS 레지스트리에 미투입)·`InternalTokenAuthenticationFilter`(3-state 보존·SIGNED_ONLY 는 평문 X-User-* **무시**)+`InternalTokenModeInvariant`(부팅 시 필터 구성 검사)·P6 삭제 8종(`JwtFilter`/`JwtTokenVerifier`/`JwtSecurityConfigurer`/blacklist lookup 2/`TokenClaims`/`TokenParseException`/`HeaderAuthenticationFilter`, **ADR-0014 D2-c exit**)·`internal-key-ownership-lint`(SPKI DER SHA-256 fingerprint — kid 대조 우회 차단, self-test 7종)+CI 배선·575 테스트 0 실패·가드 5종 그린. **Codex diff 리뷰 3 chunk 24건 → 분할 아티팩트 10건 기각·실제 결함 7건 전량 반영**(내 검증도구 false-green 3건 포함: 직접경로 Bearer 가 깨진 문자열이라 vacuous-negative → 유효 토큰+검증키 등록 / 부팅 불변식 체인 0개 fail-open / 키도메인 검사가 fixture 키 1개만 대조 → 집합 서로소). **미충족 명시**: k8s gateway 매니페스트는 CSI 키 부재로 배포 불가(PR3a→PR3b 동일 취급)·부하 하 event-loop lag 미측정(마이크로벤치 baseline 만 확정 RSA2048 p95 1.80ms/3072 3.00ms)·Layer1 동기화는 후속 docs PR 로 분리(→ ✅ [#81](https://github.com/Kimgyuilli/PeakCart/pull/81) 해소) / **PR3d-b 는 [#82](https://github.com/Kimgyuilli/PeakCart/pull/82) 에서 b-1/b-2 로 재분할**(계획서 §10, 착수 전 grep 검증 6건 — V6 뒤집힘: 클러스터 잔여 0 이라 §7 ②~④ 는 라이브 마이그레이션이 아니라 fresh deploy **리허설**, 단계 전부 실행하되 증적에 리허설 명시 / V1: CSI·개인키 매니페스트 0 → **매니페스트가 lint 보다 먼저**여야 P7 vacuous-green 회피 / loop3 #3 rollback 호환 이미지 전제 소멸 → 행렬 재작성): **b-1 ✅ [#83](https://github.com/Kimgyuilli/PeakCart/pull/83)** P5 매니페스트(gateway/user `SecretProviderClass`+CSI read-only 마운트[**user 키 ad-hoc Secret→CSI 정본화**, PR3c 편차 해소]·`internal-token-keys`/`internal-token-binding` ConfigMap 신설[공개키 PEM + 인덱스 env — 회전 overlap 을 이미지 재빌드 없이]·5서비스 배선·`application-k8s.yml` 키 경로)+P7 lint(`gateway-exposure-lint` "비밀 0"→**"승인 CSI 정확히 1개"**[SPC1↔volume1↔mount1·양쪽 readOnly·`secretObjects`/`nodePublishSecretRef`[SPC+**inline** 양쪽] 금지·resource/alias/mountPath/namespace exact]·**`workload-key-ownership-lint` 신설**[Deployment/StatefulSet/DaemonSet/ReplicaSet/Job/CronJob/Pod 전수+init/ephemeral·소유자 **(ns,kind,name)**·SPC **내용** allow-list·개인키 base64 decode PEM marker 탐지·서비스별 배선+JWT 도메인 env 오염])+P8 회전 runbook(§11)+P10 barrier ② 신설(위조·평문·**정상 서명 양성 대조군**·**gateway Pod 내 직접경로 Bearer 거부**)+`CANARY_RESULT` 서브셸 버그 수정+§7 수렴 판정식 스크립트화(`rollout-convergence-gate.sh`)+§12 rollback 행렬 재작성. **`InternalTokenPropertiesBindingTest` 가 설계 가정을 반증** — Spring 리스트는 인덱스 병합이 아니라 **최고 우선순위 소스가 통째 대체**(ConfigMap 이 신뢰 kid 집합 단일 출처 → 회전 ⑤가 실제 폐기). **Codex 리뷰 11건 전량 반영**(분할 아티팩트 0 — single 모드, 내 검증도구 false-green 3건 + 내 논증 오류 1건[§7 ③ 은 DUAL_ACCEPT 라 "Pod 200=서명 주입" 불성립] 포함). lint 10종·**self-test 69종**·렌더 3종·10모듈 그린 · **b-2 🔄 진행 중**(계획서 `task-impl3-pr3d-b2-cluster-session.md` P1~P16 — 착수 전 코드 검증 8건 중 **2건 뒤집힘**: gateway 는 평문 `X-User-*` 주입을 이미 삭제해 §7 ②/③ 리허설의 "before" 는 PR3c 시점 이미지 필요(W7) / `k8s/` 에 `ServiceAccount`·`serviceAccountName` **0건** 이라 SPC 가 규정한 Workload Identity 의 주체가 없었다(W8, P2 신설). **P2 ✅ [#109](https://github.com/Kimgyuilli/PeakCart/pull/109)** gateway/user 전용 KSA + 상이 GSA 바인딩(ADR-0017 D3)·`automountServiceAccountToken: false` 유지(CSI provider 는 드라이버 TokenRequest 로 KSA 신원 획득)·**신원 층위 우회 차단 lint WKO-012/013 신설**(Secret Manager 권한은 볼륨이 아니라 신원에 붙어 KSA 를 빌려 쓰면 CSI 전수 검사가 통째로 무력 — `ksa_borrowed_by_job` 은 볼륨 0개 변이라 기존 검사 전량 그린 상태에서만 red, self-test 29/29) — 렌더 축만 그린이던 것을 **클러스터 세션(2026-09-14/15)에서 P1~P10 실증**, 증적 `docs/progress/evidence/pr3d-b2-gke-20260914-1320.md`: P1 GKE+WI+CSI(**관리형 Secret Manager 애드온 기각** — 드라이버명 `secrets-store-gke.csi.k8s.io`/provider `gke` 가 커밋 계약과 달라 upstream helm + GCP provider v1.17.0 으로 설치) · P2 클러스터 축(GSA 2·`secretAccessor` 교차 0·WI 바인딩) · **P3 은 공개키 배포 누락을 실행 중 발견해 보정** (개인키만 새 키라 JWKS 게시 공개키와 모듈러스 불일치 → 서명 경로 파손. barrier 는 공개 경로만 봐서 못 잡는다) · P4 ① 3상태 양성 대조군(200×5→000×5→000×5)+barrier PASS · P5 ② 평문 200 실측(**W7 해소** — PR3c 이미지 GHCR 잔존) · P6 ③ 수렴 · P7 ④ 2 Pod 전부 평문 주입 0 · P8 ⑤ crypto barrier 4종 PASS(**ADR-0014 D2-c exit**) · P9 ⑥ 5서비스 `mode=SIGNED_ONLY chains=1` verifier bean 0 · P10 rollback ④ 출발 행 env 하나·digest 불변·양방향. **계획서 정정 3건**: P5 `--expect-image` 는 전체 적용이라 서비스별 실행 / P6 서명 probe 를 ③ 에 걸면 DUAL_ACCEPT 라 vacuous-green(스크립트 `assert_downstream_signed_only` 가 차단) / P10 기대식 `401→200` 은 관측 불가 — gateway 가 외부 `X-User-*` 를 strip 하므로 관측 지점을 gateway Pod 내부로 옮겨야 한다. **발견 결함 2건(별건 PR)**: ① `apply -k` 가 HPA 최소값을 깎는다(base `deployment.yml:20` `replicas: 1` 이 매 apply 마다 minReplicas 2 를 덮음 — overlay 는 주석만) ② user 키 도메인에 공개키 override seam 부재(내부 토큰의 ConfigMap+인덱스 env 대칭이 `app.jwt.rs256.public-keys` 에 없어 dev 키쌍으로 우회, 운영 전용 키 축 미증명). 증적 PR [#110](https://github.com/Kimgyuilli/PeakCart/pull/110). **세션 2(2026-09-15)에서 잔여 전부 수행 — b-2 ✅ [#115](https://github.com/Kimgyuilli/PeakCart/pull/115) 종결**, 증적 `docs/progress/evidence/pr3d-b2-gke-20260915-0854.md`: 선행 결함 2건이 그 사이 머지돼(#111 HPA replicas · #112 user 공개키 seam) **양 키 도메인 모두 운영 전용 키**로 수행 (user kid `peekcart-user-2026-09`, 개인키 Secret Manager v3 ↔ JWKS 모듈러스 일치 — 선행 세션의 dev 키 우회 해소) · **P2 음성 대조군 ✅** 권한 제거 후 새 마운트 강제 시 `IAM_PERMISSION_DENIED`/`secretmanager.versions.access` 고착 → 부여 시 Pod 재생성 없이 복구 (**IAM 전파 지연** 때문에 제거 **직후** 관측은 대조군이 못 된다 — 처음엔 정상 기동했다) · **P11 ✅** 회전 ①~⑤ 완주(선배포→수렴 게이트 5/5 두 kid 동시 수용→개인키 v2+active-kid 전환 **무중단**→80s 대기→구 키 소멸), 리스트 통째 교체로 ⑤가 **실제 폐기**임을 확인 · **P12 ✅** 역전 재현 — 실패는 '전 요청 401'이 아니라 **롤아웃 비율에 비례**(혼재 401/200/200 → 수렴 후 401×5), 게이트는 **수렴은 통과시키고 서명 probe 에서만 차단**(probe 없이는 green) · **P13 ✅** `up=7`·`_bucket` 414계열(PR4 미충족 해소, p95 NaN 아님)·`auth_failure_total` 주입 증가 확인(`malformed`/`alg_not_allowed`) · **P14 ✅ 축소 없이** — loadgen 을 VM→**클러스터 내 k6 Job** 으로 바꿔 쿼터 12 안에서 3노드 확보, 인증 p95 **23.61ms** vs 공개 **13.67ms**(오버헤드 +9.9ms)·probe 무부하 4.8ms→한도내 5.0ms(영향 거의 없음)·**auth 실패 기저율 0**(→ 비율 아닌 '증가 자체' alert)·**HPA gateway 2→4(max) 실관측**. **추가 발견 4건(후속 PR)**: ① user 회전 runbook §4.1 오류 — `active-kid` 만 되돌리면 복구 안 되고 개인키 버전도 되돌려야 하며 **버전 비활성화는 롤백 수단이 아니다**(`versions/latest` 가 DISABLED 를 건너뛰지 않아 마운트 자체 실패 → 구 키 **재업로드**가 정답) ② 서명 probe 진단 메시지가 오진 유도('구 이미지가 평문 주입' — 실제는 kid 불일치) ③ 계획서 P13 음성 대조 기대값 오류(selector 완화만으로는 타깃 불변 — `endpoints[].port` 가 2차 필터) ④ 계획서 P14 에 **rate limit 전제 누락**(첫 측정 60% 실패가 전부 429, 한도 40 req/s — 한도 위 측정은 수치가 통째로 오염) / **PR4 ✅ [#108](https://github.com/Kimgyuilli/PeakCart/pull/108) 인증 관측성 S9 + HS512 잔재 제거** — S9 는 ADR-0009 §Decision 에 2026-05-04 부터 행만 있고 코드는 0건이었다. `auth.failure{reason}`(거부 단일 지점 `reject()`, **401 세부 사유는 메트릭에서만 분해** — 응답은 `invalid_token` 으로 합쳐 위조 시도에 피드백 주지 않음, 사유는 enum 이라 예외 message 가 태그가 되지 않음)·`auth.ratelimit.rejected{route}`(한도 초과 전용 — 판정 불가 503 과 분리)·`auth.forbidden{route}`(gateway 는 authz 를 안 하므로 **다운스트림 403 관측**, 5서비스 복제 대신 이름 1개소)·user `auth.token.reuse.detected`/`auth.logout`(CommitAwareMetrics — 롤백 시 미증가). **착수 전 범위 재확정**: gateway 에 S1(histogram)·S2(application 태그)가 **둘 다 없어**(:common 미의존 여파) 이 상태로 alert 에 넣으면 p95 가 NaN → 관측성 기반 배선이 S9 보다 선행. **canonical 5→6 은 ADR-0015 §Decision 계약 변경이라 ADR-0024 신설**(세 집합 분리 — 태그 `gateway` / scrape Service `gateway-metrics` / SM `gateway`. A≠B 를 허용한 것이 결정의 실질: 억지로 맞추면 관리 포트를 public Service 에 게시하거나 태그가 Service 이름을 흉내내야 한다). `gateway-metrics` ClusterIP Service + SM 신설·alert regex 6값 + scrape-absent + **reuse alert 신설**(401 은 baseline 부재로 비대상, reuse 는 발생 자체가 사건)·dashboard 정정(kafka-lag 의 소비자 0건 `user-service` 제거)·**lint 4종**(promql 상수 3분해 + **dashboard 검사 신설**[무검증 표면이었다] self-test 10→17 / servicemonitor-selector **self-test 신설 6종**[음성 대조군 0이었다] / gateway-exposure "Service 1개"→"승인 2개" 25→29 / ssot-lint 에 gateway 편입). **P22 전제 뒤집힘**: 발급측이 이미 RS256 단독이고 legacy `bl:` writer 가 0 이라 "TTL 경과 증명 게이트" 불필요 → HS512 fallback·dual-read·`app.jwt.secret`·`JWT_SECRET`(k8s 5)·미사용 jjwt test dep 즉시 제거. **구현 중 뒤집힌 전제**: 의존성 추가만으로 S1 이 배선되지 않는다(`MetricsConfig` 는 `global.config`, scan 기점은 `gateway`) — `http_server_requests_seconds` 는 나오는데 `_bucket` 만 없어 **p95 alert 만 조용히 NaN**. 실제 스크랩 출력을 검사한 통합테스트가 반증. **미충족**: 실 클러스터 scrape 증적(PR3d-b-2 동일 세션)·부하 하 auth failure baseline·Codex 리뷰 미호출(사용자 지시) | A4 | 보안 묶음 | ✅ |
| ④ | Choreography Saga (착수 전 코드 검증에서 **범위 재확정** — ADR-0012 ④ 산출물 4개 중 2개는 strangler-1~5 로 이미 완료, 실질 잔여는 "이미 도는 saga 의 미결 종료 상태" 닫기. 계획서 `task-impl4-choreography-saga.md` P1~P15, PR ④-a/b/c/d 분할 · **④-a ✅ [#84](https://github.com/Kimgyuilli/PeakCart/pull/84)** L-013 실측(결정적 재현 — `@Version` 부재 시 취소 선커밋→`PAYMENT_COMPLETED`·결제 선커밋→`CANCELLED` 양방향 lost update, 전이 가드는 스냅샷 기준이라 무력) → `Order @Version` 승격 + 충돌 정책(타임아웃 취소=재시도 포기 / 결제완료가 취소 주문에 도착=`ORD-003` DLQ 대신 보상)+**예약 lease 계약**(Product 부여 → `stock.reservation.result` 공유 → Order 선취소 · Payment 만료 승인 거부 `PAY-010` · sweeper 는 만료+유예 후 회수, 고정 TTL 은 살아있는 주문 재고를 뺏어 oversell)+**"예약 확정·결제 미시작 PENDING" 수명 상한 부재 해소**(기존 2개 만료 조회가 못 비우는 구간, 갭 존재 증명 테스트 동반)+**`PAID_BUT_CANCELLED` 영속 보상 원장**(V4 — Slack 은 order-service 에서 no-op 이고 `processed_events` 커밋으로 재소비 불가라 알림은 종결 근거가 못 됨)+legacy backfill 3종·인덱스·배치 상한. **Codex 리뷰 5건(P0 1) 전량 반영** — P0 가 "oversell 을 닫았다"는 초안 주장을 반증(승인 검사 후 **같은 트랜잭션**에서 PG 호출, 그 시점 주문은 아직 `PENDING`) → 승인 마진으로 창만 축소하고 **fence 미구현을 계획 §2.6 R-1 로 명시**(진짜 fence 는 saga 프로토콜 변경 → ADR 선행 별도 PR). 604 테스트·lint 10종 그린 · **④-b ✅ [#85](https://github.com/Kimgyuilli/PeakCart/pull/85)** 이벤트 계약(P5·P6·P7) — `OrderCancelledPayload` 에 `items[]`(ADR-0012 D2 명문 이행)+`OrderCancelReason` enum (USER_REQUESTED/RESERVATION_FAILED/PAYMENT_FAILED/TIMEOUT)·`publishOrderCancelled(order, reason)` 로 **취소 진입점 4곳이 사유 명시 전달**(발행기 추론 금지)+**P7 결정: `payment.failed` 취소도 `order.cancelled` 발행**(ADR-0010 D3-4 복원 — 재고 복구는 Product 가 `payment.failed` 직접 소비로 이미 수행하므로 중복 트리거지만, Product=원장 CAS no-op·Payment=`FAILED` no-op·Notification=`reason=PAYMENT_FAILED` 스킵으로 무해. **중복 차단 장치가 P6 의 `reason` 이라 P6 없이 P7 은 불성립**)+소비자 3곳 하위호환(필드 부재·모르는 값은 알림 발송 쪽). **Codex 리뷰 2건(P0/P1 0 — 자동통과였으나 전량 반영)**: ① `handlePaymentFailed` 무조건 `cancel()` → 취소 선커밋 경합에서 `ORD-002` 롤백·DLQ 행(P7 발행으로 대가 증가) → `CANCELLED` no-op 종료 ② **계획 §5.2 의 '동일 트랜잭션 Outbox' 검증이 false-green**(`@InjectMocks` 는 Spring 프록시·DB 부재라 `@Transactional` 을 떼도 통과) → `OrderCancelledEventContractIntegrationTest` 신설(동시 커밋 + `@MockitoSpyBean` Outbox 실패 주입 시 상태·`processed_events` 전량 롤백). 617 테스트·lint 10종 그린. Layer 1 동기화는 P15(④-d) 이연 · **④-c/d 🔲 대기**, ④-c 는 R-2 배포 의존성(원장→환불) 있음. **④-c 착수 전 코드 검증에서 범위 확대 확인** — 계획서 P8 은 Product `PAID_BUT_UNRESERVED` 만 지목하나 실제 감지는 **3지점**(Product marker·Order 영속 원장·**Payment 비영속**)이고 `TossPaymentClient` 에 취소 API 부재·`APPROVED` terminal → **선행 ADR-0018 ✅ [#86](https://github.com/Kimgyuilli/PeakCart/pull/86)** (보상/환불 트리거 계약: 토픽 3+DLQ 3·`payment_refunds` 원장·**dispatcher 분리 fence**·결과별 종착·조회 기반 복구·backlog 게이지, ADR-0012 처분=refine). 이후 **④-c-1**(P8 구현) → **④-c-2**(P9·P10·P13 나머지)로 재분할. **④-c-1a ✅ [#87](https://github.com/Kimgyuilli/PeakCart/pull/87)** 환불 실행 엔진(payment-service 내부) — `payment_refunds` 원장(`order_id` UNIQUE = 동일 논리 환불 1건 fence·`generation` fencing token)+`RefundStatus` 상태머신+**진입점/실행자 분리**(소비 트랜잭션에서 PG 를 부르면 성공 후 롤백 시 fence 행이 사라져 fence 자체가 무효 → 진입점은 REQUESTED 커밋만, PG 는 CAS claim 한 dispatcher 만)+**reconciliation 조회 선행**(crash matrix 4칸: claim 후 사망·성공 후 커밋 전 사망·타임아웃·REQUESTED 직후 사망)+`ALREADY_CANCELED` 조회 3분기+결과별 종착 분리(SUCCEEDED만 payments=REFUNDED+회신 / FAILED는 회신하되 APPROVED 유지 / UNRESOLVED는 회신 없음)+`payments.user_id` NOT NULL 전환(가드 선행)+메트릭 5종+`payment.refunded` 계약/토픽. **실측 정정**: `ON DUPLICATE KEY UPDATE id=id` 가 중복에도 1 반환(Connector/J 기본 found-rows) → `INSERT IGNORE`. **Codex 리뷰 2라운드 18건 전량 반영** — 라운드1 10건(stale CLAIMED 재호출 경로 부재+**그걸 통과시킨 내 테스트**·pg-timeout 미적용·user_id nullable·finalize 경합 등), 라운드2 8건(**2건은 라운드1 수정이 만든 새 결함** — generation TOCTOU·UNRESOLVED claim 이 상태 미변경으로 진행 중 건 수동 종결 허용). 651 테스트·lint 10종 그린. **R-2 미해소**(회신 소비는 1b) · rollout gate = 1a/1b 같은 릴리스 주기 · **④-c-1b ✅ [#88](https://github.com/Kimgyuilli/PeekCart/pull/88) R-2 해소** 크로스서비스 계약(P10~P15) — 요청 토픽 2종 신설(`stock.compensation.requested`/`order.compensation.requested` +dlq, producer-owns-topic)+`CompensationRequestedPayload`/`CompensationReason`(:common, **금액 미탑재** — 금액 결정 주체는 Payment)+Payment 소비 2경로가 **로컬 감지와 같은 fence 로 수렴**(cross-topic 순서 무보장이라 순서 의존 로직 0, 요청2×로컬 선후·중복 조합 전수 고정)+**트리거 발행 2경로가 감지 기록과 동일 트랜잭션**(Product marker CAS·Order 보상 원장 ↔ 요청 Outbox, `detectedAt`=원장 시각 그대로 — `markCompensatedIfAbsent` 가 타임스탬프를 호출자에게서 받도록 변경)+**backfill**(Product V4·Order V5, 멱등 근거 = **producer DB 안의** `NOT EXISTS(aggregate_type+aggregate_id+event_type)` — Payment UNIQUE 는 DB 경계를 넘지 못한다 / **2단계 분리**: 한 SELECT 의 `UUID()` 2회 호출은 값이 갈라져 eventId 가 두 자리에서 불일치 → 컬럼 확정 후 envelope 조립, 중간 실패에도 재실행 안전)+**회신 소비 3곳 종결**(Order `RESOLVED`/**`REFUND_FAILED`+`failure_code`** 신설 — 실패를 RESOLVED 로 닫으면 원장이 거짓말을 한다 / Product 종결 컬럼 3개 — `compensated_at` 은 감지 marker 이지 종결이 아니다 / **`UNRESOLVED` 는 어느 소비자도 전이하지 않음**)+**Notification 은 `SUCCEEDED` 만 알림**(실패·미확정을 사용자에게 전가하지 않음, `PAYMENT_REFUNDED` 신설)+구 메시지 내성(필드 부재·미지 값)+부모 계획 P8/§7 재분할·**P12 에 환불 체인·P14 에 결과/crash 매트릭스 등재**(실행은 ④-d)+Layer 1 동기화(03 보상 요구사항·04 토픽 매트릭스 10종 정정·05 `payment_refunds`/`order_compensations` 등재). **Codex 3-chunk 리뷰 14건 → 13건 반영·1건 기각**(기각=분할 아티팩트) — P0 는 **`payment.refunded` 선도착 시 원장이 영구 미결**(fence 가 요청을 흡수한 뒤 회신이 없는 경우를 계약에서 놓침) → 종결된 fence 에 요청이 오면 **회신 재발행**. 그 외 backfill 중간행의 poller 노출(→`BACKFILL` 상태 신설, 2단계가 payload+`PENDING` 원자 전환)·요청 소비 fail-open(필수 필드·양수 검증)·Notification `asLong()` 축약·종결 불변식 미강제·**false-green 4건**(listener 배선 직접 호출, backfill 테스트가 마이그레이션 SQL 복제본 실행 → 테스트가 V4/V5 파일을 직접 읽도록 SSOT 통합). **697 테스트 0 실패**(신규 45건) · lint 10종 그린 — Outbox 실패 주입 시 감지 기록까지 롤백을 `@MockitoSpyBean` 으로 판정. **미충족**: 크로스서비스 E2E 는 ④-d(부모 P12) · Toss 실호출은 D-020 게이트 · DLQ 원장/runbook 은 ④-c-2) · **④-c-2 는 계획 리뷰 3라운드(12→8→10건, 전량 반영)에서 2a/2b 로 분할** — 3R 결함 10건 중 6건이 replay 경로였고 **"재발행 중복 0" 이 2R(2단 상태머신)·3R(outbox 재사용) 연속 반증**(`OutboxPollingService:83-86` 이 broker ack 후 별도 커밋 → 같은 crash window)되어 ADR 사안으로 판정 · **④-c-2a ✅ [#90](https://github.com/Kimgyuilli/PeakCart/pull/90)** DLQ 원장 적재 — 물리 식별자 6컬럼 (`cluster_id`+`topic_generation` = 토픽 재생성 시 offset 0 재사용이 `INSERT IGNORE` 로 조용히 폐기되는 것 차단 / `failed_consumer_group` = 공유 DLQ 에서 소유권 분리, **전부 NOT NULL** — nullable 이면 MySQL UNIQUE 가 poison record 를 여러 행으로 만든다)+`DLQ_ORIGIN` 종류 분리(origin 헤더 판독 불가 시 DLQ 자신의 좌표)+**quarantine 전용 listener**(발행 서비스가 group 부재분 단일 소유자인데 자기 `.dlq` 를 구독하지 않아 볼 수 없던 자체 반증 해소, 두 소유권 집합 교집합 ∅ 계약 강제)+**`ackAfterHandle=false`**(기본값이면 원장에 못 쓴 레코드가 offset 커밋으로 영구 유실 → 무유실 택하고 재시도 소진 시 컨테이너 정지)+보존/age·backlog 경보/actuator 조회·종결 진입점+`dead-letter-schema-parity-lint`+runbook. **계획 전제 3건이 리뷰로 반증**(jar bytecode 로 확인: `HeadersToAdd.GROUP` 기본 포함이라 커스텀 헤더 불필요 / eventId 없는 메시지가 DLQ 대표 입력 / `DLT_ORIGINAL_KEY` 부재) · **구현 중 자체 발견**: runbook 이 `UPDATE SET status` 를 지시해 `discard()` 사유 가드를 우회 — 3R #7 의 false-green 구조를 종결 경로에서 재현한 것이라 진입점 신설. 751 테스트·lint 11종 그린. **미충족**: replay(2b) · "정확히 1곳" cross-service 증명(④-d P12) · 메트릭/E2E/게이트(④-d) · **④-c-2b 🔄 **4분할 진행 중**(계획 리뷰 3R 40건 전량 반영 — `task-impl4-c2b-dlq-replay.md`. 2b-1 원장 축 → 2b-2 발행 표면 → **2b-3 재실패 상관을 진입점보다 먼저** → 2b-4 진입점·backfill·문서. 순서를 뒤집은 이유: 진입점을 상관보다 먼저 열면 그 구간의 재발행 실패가 독립 incident 로 갈라져 ADR-0020 D5-4·D6-3 위반) — **2b-1 ✅ [#100](https://github.com/Kimgyuilli/PeakCart/pull/100)** 원장 replay 축 + incident 집계 정정: 4 DB additive 마이그레이션(order V8·product V6·payment V6·notification V4, 11컬럼 전부 nullable)+`publication_status`↔`status` **두 축 물리 분리**(PUBLISHED 는 terminal 아님 — 발행 성공은 사건 해소가 아니다)+집계를 행→**incident(root)** 로 전환(단 `root_record_id = id` 로만 걸면 **④-c-2a 적재분이 전부 탈락해 backlog 0** 이 되는 반대방향 false-green → `IS NULL` 분기 병기)+`RESOLVED`(근거 필수)+종결의 **root 정규화·활성 자식 전파**+purge 를 상태별 `CASE`(COALESCE 는 DISCARDED→재개방→RESOLVED root 에서 과거 시각을 골라 조기 삭제)+parity lint 를 신규 마이그레이션·java 9파일 복제까지 확장(self-test 4종→**10종**). **diff 리뷰가 실제 버그 2건**을 잡았다 — **JPA 1차 캐시가 잠금 후 재검사를 무력화**한다(엔티티 선읽기 시 `FOR UPDATE` 가 잠금만 얻고 refresh 안 함 → id projection / 자식 미잠금 시 REPEATABLE READ 스냅샷을 봐 **root 는 no-op 하면서 자식만 덮어씀** → `findChildrenForUpdate`). 3R P1=0 수렴, 918 tests 0 실패, **변이 8종 red**(그중 M3 은 처음 green — purge COALESCE 회귀 테스트가 인메모리 재검사에 가려 vacuous, 쿼리 단언 추가로 red). 미충족: V-21c(purge 재검사 2-트랜잭션 경합)는 재개방 경로가 2b-3 에 생겨 그 PR 에서 검증 · 신규 테스트는 order 만(나머지는 byte 동일 + lint) · `NOT NULL` contract 는 2b-4 backfill 이후 · 운영 클러스터 미적용 — **선행 ADR-0020 ✅ [#98](https://github.com/Kimgyuilli/PeakCart/pull/98)**(D1~D7 확정 + 계획 검증에서 드러난 **D8 발행 권한 예외** 추가. D1=publication at-least-once("중복 발행 0" 계약 삭제 — Kafka 트랜잭션은 DB↔Kafka 원자성이 아니라 **비해결 대조군**) · D2=notification outbox 신설(ADR-0012 D1 개정) · D3=outbox additive 컬럼+kind 분기 · D4=retention 7종 config 계약화, **"7일 좌표 가용"은 best-effort 강등**(PVC 1GiB 안전성 미증명 — `__consumer_offsets` 50파티션·KRaft metadata 가 bound 밖) · D5=금지축 **6개 독립조건**(`origin_kind='DLQ_ORIGIN'` replay 시 **`.dlq` 내용이 원본 토픽에 주입**) · D6=`publication_status`↔`status` **물리 분리**, 종결은 사람만 · D7=Actuator `@WriteOperation` · D8=`1 topic=1 producer` 명시적 예외+fence 6종, **ADR-0012 D4·ADR-0018 부분 무효화**. **착수 전 검증이 원 계획 전제를 정정** — "브로커 retention 미설정"은 선언 기준으로만 참이고 실효값은 Apache 기본 7일(`server.properties:105`)이라 P4 는 동작 변경이 아니라 **계약 명문화**다. 계획 리뷰 3R(13→9→15)+diff 리뷰 3R(7→6→7) **57건 전량 반영·양쪽 미수렴 종료** — 매 라운드가 직전 라운드 수정이 만든 결함을 잡았고, 내 false-green 3건 포함(P4-4 "사실대로 적으면 통과" · 축 A/B 를 선언만 분리하고 단일 `status` 컬럼에서 재결합 · **`root_record_id=id` 조건이 기존 NULL 행을 전부 탈락시켜 backlog 를 0으로 만드는 반대방향 false-green**). **④-c-2b-0 ✅ [#99](https://github.com/Kimgyuilli/PeakCart/pull/99)** 브로커 retention 계약 실설정 — 20토픽(업무 10+dlq 10)에 config 7종 선언 + `modify-topic-configs`(NewTopic 소유 3서비스) + 안전여유 fail-fast(`retention ≥ dlqReplayWindow+clockSkew+cleanupSafety`, 4서비스 7d→9d, budget 양수 강제 — 0이면 required 가 dlqReplayWindow 로 내려가 제거하려던 7d==7d 가 다시 부팅). **`segment.bytes` 선언이 필수**였다 — `retention.bytes` 는 닫힌 세그먼트만 지우고 active segment 가 그 위에 얹히는데 이미지 기본이 1GiB 라 파티션 하나가 PVC 전체를 넘길 수 있다. **동작 변경 여부를 나눠 기록** — `retention.ms`/`cleanup.policy`/`message.timestamp.type` 은 실효 불변(이미 Apache 기본값, V-P4-1 런타임 관측으로 승격), `retention.bytes`/`segment.*`/`before.max.ms` 는 동작 변경. `before.max.ms` 는 리터럴 8d 대신 `app.idempotency.retention` 유도(두 곳에 적으면 floor 변경 시 갈라짐 — 구현 전 계획·ADR 선수정). **764 테스트 0 실패**(5모듈) · 변이 4종 red 실측(configs 제거 / 등호 복귀 / YAML 키 제거 / 8·4→10·2MiB). diff 리뷰 1R 5건 전량 반영 — **내 테스트가 SUT 상수를 기대값으로 읽는 자기대조**여서 합만 같으면 통과했다(→ADR 값 리터럴 독립정본), budget 0 허용 버그, 메커니즘 테스트가 YAML 배선 미검증. **2R 미실행**(상한 아님 — 미충족에 명시). **④-c-2b-2 ✅ [#102](https://github.com/Kimgyuilli/PeakCart/pull/102)** 발행 표면 — outbox replay kind + notification outbox 신설: 3 DB additive(order V9·product V7·payment V7, replay 컬럼 9개 **전부 nullable·DEFAULT 없음** — nullable 은 롤링 배포 중 구버전 INSERT 를 살리고, DEFAULT 부재는 판별자 누락이 조용히 DOMAIN 으로 분류되는 것을 막는다. **읽기는 NULL 을 DOMAIN 으로 관대하게, 쓰기는 항상 명시** — 비대칭이 의도)+notification `outbox_events` 신설(V5·`global/outbox` 8파일, ADR-0020 D2 — **소비 전용 서비스가 outbox 를 갖는 이유는 replay 가 원장 소유 서비스의 자기 원장 재발행이기 때문**이고, 남의 토픽에 write 하는 근거는 **D8 의 명시적 예외**다. `NewTopic` 소유 불변이라 ADR-0011 D2 무영향)+`event_type` 에 **`__replay__` sentinel**(destination topic 을 넣으면 롤백 시 구 poller 가 **원장 id 를 key 로 fence 없이 업무 토픽에 발행**한다)+poller kind 분기(**도메인 `buildRecord()` 불변**)+**발행 축 reconciler**(전이 주체 1종·`PENDING` 은 종착 금지·**사건 축 불가침** — 발행 성공은 사건 해소가 아니다)+**outbox 부재를 `PUBLISH_FAILED` 로 추론하지 않음**(부재는 실패의 증거가 아니다 — 이미 발행된 행이 cleanup·수동 삭제로 사라져도 똑같이 관측되고, 자동 강등하면 발행된 사건을 실패로 감사 기록하고 재요청까지 열어 같은 메시지를 두 번 낸다)+cleanup 이 `REQUESTED` root 연결분 제외+ADR-0012 D1 표 Update Log(`fix(adr):`). **착수 전 코드 검증이 전제 5건을 뒤집음** — **C-5 "parity lint 가 대조한다" 는 거짓**(기존 lint 는 `dead_letter_records` 전용, `outbox_events` 를 보는 검사 0) → P9-b 신설 · **C-6 은 리뷰가 내 정정을 재반증**(“단일 테이블 DELETE 에 alias 불가”가 거짓, MySQL 8.0.16+ 는 허용 — `mysql:8.0.46` 실측 4행 매트릭스. 초안 SQL 이 깨진다는 결론은 유지되나 이유가 **alias 미선언**이었다) · C-12 ADR-0012 미갱신 · C-3 stale 주석 2→6곳(`NotificationApplication:12` 는 **이미** 거짓) · C-8 `cleanup.cron` 은 3서비스에도 없는 키. **구현 중 자체 발견**: `DeadLetterPublicationReconciler` 를 4벌 복제해 놓고 parity 목록에 안 넣어 **④-c-2b-1 의 glob 미확장과 같은 구멍을 재발**시켰다 → 개별 self-test 대신 **`DLQ-PARITY-014`(4벌 byte 동일인데 목록에 없는 파일)로 목록 자체를 검사**, 그 검사가 **기존 미등록 복제본 2개**(`DeadLetterContainerGuard`·`DeadLetterKafkaConfig`)까지 찾아 편입. 디렉토리 전체 일치를 요구하지 않는 이유는 consumer/config 3종이 서비스마다 정당하게 다르기 때문(음성 대조군 고정). **내 fixture 가 현실을 왜곡한 것 2건**도 함께 수정(per-service 파일을 order 사본으로 채워 fixture 안에서만 동일해지던 것 · `$TMP` 미삭제로 앞 케이스가 뒤 케이스를 red 로 만들던 것). **at-least-once 단언이 과했다** — "정확히 2개" 인데 실측 3개(배경 poller), D1 은 중복 수에 상한이 없으므로 계약이 말하지 않는 것을 주장하던 flaky. **902 tests 0 실패**(모듈별 — 로컬 전체 스위트 완주 실패로 나눠 실행)·**변이 14종 red**·lint 7종 green(parity self-test 10→**18종**). **최초 CI 가 `test (order-service)` 실패** — **운영 코드 결함 0, 전부 이 PR 이 만든 테스트 하네스 결함 4건**이었다. 확정 증거는 실패 시 `brokerEndOffsets` **전부 0**(소비 실패가 아니라 **1사이클 send 자체가 실패** — 컨테이너 다수 기동 시 토픽 생성 전에 첫 send 가 나가 메타데이터 타임아웃). 수정: Mockito 재스터빙→`AtomicBoolean` 단일 answer(**CI 실패 직접 원인** — 재스터빙 구간에 배경 `@Scheduled` 호출이 겹치면 새 answer 미반영) · `awaitTopicReady` · 측정을 소비→**broker end offset**(group 조인·리밸런스·fetch 타이밍 제거) · **1사이클 ack 명시 단언**(이게 없어 첫 발행이 조용히 사라져도 통과하고 엉뚱한 곳에서 터졌다). **진단 중 내 추론 2건이 틀렸다** — `hasMessage("DB down")` 은 send 성공의 증거가 아니고(`handlePublishFailure` 끝 save 가 덮어쓴다), `fixedDelay` 는 첫 실행을 막지 못한다. **계획 C-9 판단 반전**(`@Scheduled` 리터럴 유지 → `fixedDelayString` 설정화, 운영 기본값 5s 불변) — 배경 잡이 도는 상태에서는 발행 횟수를 세는 테스트가 구조적으로 성립하지 않는다. 두 클래스 동시 **6회 연속 통과**(느린 실행 포함) 후 **CI 전면 green**(test 6·lint·guards·gate·images 6·**e2e**). **미충족**: **Codex diff 리뷰 미실행**(usage limit — P0/P1 0 이 아니라 **미측정**) · replay 행은 fixture 로만 생성(진입점=2b-4) · `NOT NULL` contract 미수행(R1) · `05:177` 정정은 P24 · 운영 클러스터 미적용 · **④-c-2b-3 은 계획 리뷰 2R(17+9건 전량 반영) 후 3a/3b 로 분할** — 2R 이 ADR 신설·4서비스 마이그레이션·lint 재구조화를 더해 P14 하나가 이미 PR 한 개 분량이 됐고, 계획 3R 이 quota 로 막혀 한 덩어리면 diff 리뷰도 같은 한도에 걸린다 · **④-c-2b-3a ✅ [#103](https://github.com/Kimgyuilli/PeakCart/pull/103)** replay 상관 표면 — **ADR-0021 신설 + ADR-0020 `Partially Superseded`**(§D5-4 가 대조에 `record_kind=REPLAY` 를 요구하는데 그 컬럼은 `outbox_events` 전속이라 **같은 절이 배제한 수명 경쟁에 그대로 걸린다** — ADR 내부 충돌이었고, 대조 축을 원장 앵커+fingerprint+**payload digest** 로 대체. Update Log 가 아니라 새 ADR 인 것은 `adr/README.md:14` 가 트레이드오프 변경의 Update Log 우회를 금지하기 때문)+`ReplayHeaders` 키 정본 4종(`common`, 1벌 — `global/deadletter` 4벌 복제를 늘리지 않는다)+**송신 측 키 집합 정확 일치 강제**(부분집합 허용으로는 부족 — `replayHeaders()` 가 빈 JSON 에 빈 Map 을 주고 `addHeaderIfPresent` 가 blank 를 생략해 **헤더 0~3개짜리 replay 가 발행**된다)+판독(`DlqOrigin` +4필드·`claimsReplay()`, **root-id 파싱 실패는 예외가 아니라 null** — 조작 가능한 헤더 하나가 DLQ 적재를 막으면 실패 사실이 유실된다. **쓰기는 엄격·읽기는 관대**가 의도된 비대칭)+앵커 5종 매핑+`last_replay_payload_digest` 4 DB additive(order V10·product V8·payment V8·notification V6 — **원장 `payload` 는 8000자로 잘려 저장돼 대조 불가**, 절단분 비교는 상한 밖 변조를 통과시켜 같은 eventId 의 **변조 payload 가 남의 사건을 재개방**한다)+**parity lint 를 glob 기준 → 최종 스키마 합성 기준으로 전환**(기존 검사는 `V*__dead_letter_replay_axis.sql` 한 파일만 보고 컬럼도 하드코딩이라 **별도 V10 파일은 본실행·self-test 전부 green** — ④-c-2b-1 glob 미확장, ④-c-2b-2 java 목록 누락에 이은 **같은 구멍 세 번째**. self-test 18→**23종**). **착수 전 코드 검증이 전제 3건을 뒤집었다** — `pc-replay-*` 상수는 **코드에 0건**(계획서 2줄이 전부) · `DlqHeaders` 에 allowlist **자료구조 없음** · V8 이 만든 앵커 컬럼 **미매핑**. 반대로 대전제(커스텀 헤더가 재실패 시 DLT 까지 생존)는 `spring-kafka-3.3.14` **바이트코드로 확인**(`accept()` 가 원본 헤더 전량 복사 후 `DLT_*` 를 얹는다). **계획 리뷰가 그 검증 표 자체를 4건 반증** — C-18 “마이그레이션 0”(→digest 필요) · C-24 “호출부 2곳”(→**실측 44곳**, notification 엔 quarantine consumer 없음 → 시그니처 확대 폐기, `LedgerOwner` 빈 주입으로) · C-27 “누락은 자동으로 막힌다”(→`DLQ-PARITY-014` 는 **4벌이 이미 동일할 때만** 신고) · “기존 best-effort 계약”(→`record()` 는 `@Transactional` **안에서** Slack 호출, javadoc 이 이미 거짓). **2R 9건은 전부 1R 수정이 만든 결함** — digest **writer 미배정**(실제 replay 에서 root digest 영구 NULL → 대조 축이 늘 통과) · 마이그레이션 번호가 P23 backfill 과 **충돌** · 송신 “부분집합” · ADR Update Log 우회 · 롤백 drain 을 `REQUESTED==0` 으로 판정(ack 시 `PUBLISHED` 로 바뀌어 재시도 중 레코드를 못 잡는다) · “lint 본실행이 digest parity 를 강제” 거짓. **1006 tests 0 실패**(8모듈)·lint 15종·parity self-test 23종·**변이 1종**(`requireComplete` 제거→거부 4종 red). 그 변이가 **내 테스트의 격리 결함**도 잡았다 — 같은 토픽을 쓰는 다른 테스트의 `allSatisfy` 가 함께 깨져 엉뚱한 곳에서 red 가 났고, record key 로 걸러 재변이 시 정확히 4종만 red. **미충족**: **계획 3R·diff 리뷰 둘 다 미실행(quota, 재개 9/7)** — P0/P1=0 이 아니라 **미측정**이고 #102 에 이어 2개 PR 연속이다. **PR 분할이 이 문제를 해소하지 못했다**(분할 전제는 “각 조각이 한도 안에서 리뷰된다” 였으나 한도는 조각 크기가 아니라 1R/2R 이 소진한 **잔량**에 걸렸다) → 2b-3b 착수 시 #103 diff 포함 재리뷰 선행 · digest writer 는 2b-4 P21 · 상관 로직은 2b-3b · 운영 클러스터 미적용 · **④-c-2b-3b ✅ [#104](https://github.com/Kimgyuilli/PeakCart/pull/104)** 원자 상관 + 재개방 — `DeadLetterRecorder` 6단계(로케이터→root `FOR UPDATE`→**9축 대조(유일)**→자식 INSERT→**current read** 재조회→연결·재개방). **실행 순서를 초안에서 바꾼 이유**: `insertIfAbsent` 의 `clearAutomatically` 가 INSERT 직후 컨텍스트를 비워 앞서 잠근 root 가 detach 되고 뒤따르는 `reopen()` 이 dirty checking 대상이 아니라 **UPDATE 가 안 나간다**(자식 연결만 보는 테스트면 green 인 채로 재개방이 사라진다). **단계 5 는 `findByIdForUpdate` 여야 한다** — 일반 `findById` 면 REPEATABLE READ 에서 단계 1 이 연 스냅샷을 다시 읽어 그 사이 닫힌 root 를 `OPEN` 으로 보고 재개방을 건너뛴다. **attempt 대조는 단계 3 한 곳뿐**(관측점을 늘리면 서로 은폐해 단일축 변이가 성립 안 함) · **group 3자 대조**(헤더를 빼면 `pc-replay-target-group` 판독이 죽은 데이터가 된다) · **digest 는 절단 전 전문**(원장 `payload` 는 잘려 저장돼 상한 밖 변조를 통과시킨다). `LedgerOwner` **빈 주입**(4벌 복제라 하드코딩 불가 + 인자로 받으면 호출자가 소유자를 고른다 → `record(DlqOrigin)` 시그니처 불변, 호출부 44곳 그대로) · 알림을 **afterCommit + 결과별 4행**으로(기존은 `@Transactional` 안에서 Slack 을 불러 commit 실패 전에 나갈 수 있었고, 상관된 자식에도 "신규 미결 1건"을 보냈다) · Counter 2종은 `CommitAwareMetrics` 경유(트랜잭션 안에서 올리면 rollback 분까지 세도 green) + `reason` enum bounded. **ADR-0021 Update Log**(P17-b): §D2 가 관통 검증을 `V-30` 으로 참조했으나 그 ID 는 **이미 머지된** `OutboxReplayPublicationIntegrationTest` 가 쓰고 있어 → 관통=`V-35`·호환성=`V-30` 유지·`V-34` 철회. **계획 리뷰 3R~5R 전량 반영, 6R 은 사용자 지시로 중단**(수렴 미달·미측정). **4R 이 P1 8건으로 발산**했는데 원인이 **3R 반영이 3b 경계를 넘어 2b-4 의 P24 를 확장한 것**이었다 → drain 판정식·preflight 진입점을 통째로 2b-4 로 되돌렸다(`FixedSequenceBackOff` 에 `maxAttempts` 가 없어 계획의 식 자체가 불가였다). 5R 6건 중 5건도 4R 반영 자체의 결함(축 9 를 '필수 대조'로 바꾼 것이 **Accepted ADR-0021 §D2 침범**). **diff 리뷰 1R 8건 전량 반영 — 리뷰가 실제 테스트 실패 2건을 잡았다**(로컬 `gradle test` 가 XML 쓰기 실패로 `BUILD FAILED` 만 남기고 **테스트 실패를 가리고 있었다**). 나머지 P1 3건도 '테스트가 계약이 아니라 통과를 기술' 유형(`V-15c` vacuous · `V-21c` 부재 · rollback/V-16 누락). **변이 16종 red**(M3/M4 가 `V-19d`/`V-19d2` 만 red → 3자 대조 매트릭스 분리가 실효). lint 15종 green · parity self-test 23종. **미충족**: #103 diff 재리뷰 스킵(사용자 지시, P0/P1 **미측정**) · 계획 리뷰 수렴 미달 · ~~로컬 완주 미관측~~ **해소**(PR 후 완주 — **1043 tests 0 실패**, 8모듈 1h25m. 앞선 실패 2건은 자원 경합이 맞았다) · `V-19o` 는 fixture 전용(§D2↔§D5-2 어긋남, 새 ADR 필요) · 진입점 관통은 `V-35`(2b-4) · `replay_deadline`/`policy` 매핑·`NOT NULL` backfill 은 2b-4) · **④-d 는 계획 리뷰 2라운드(12+9건, 전량 반영)에서 d-1/d-2 로 분할** — 2R 9건의 영역 분포가 E2E 5·게이트 3·메트릭 1 로 갈렸고 P11 은 E2E 인프라 무의존 · **④-d-1 ✅ [#91](https://github.com/Kimgyuilli/PeakCart/pull/91)** saga 관측성 — product 5종·order 타임아웃 3종(`reason` 분리, 합치면 어느 잡이 도는지 모른다)+보상 `detected` Counter·**`backlog` Gauge**(누적 Counter 로는 잔량을 못 세 alert 데이터 series 가 없다)·DLQ `backlog`/`oldest.age` 4서비스+미결 alert 2종+`observability-promql-lint` 확장. **계획 리뷰가 뒤집은 전제 4건**(E2E SQL 이 실제 스키마와 불일치 / 매트릭스 게이트가 논리적으로 불가능 / `TossPaymentClient` baseUrl 하드코딩 / CI 이미지가 PR 에 전달 안 됨). **diff 리뷰 3라운드 13건 전량 반영(5→5→3)** — 각 라운드가 직전 수정이 만든 결함을 잡았다: ① Counter 가 커밋 전 증가 → `CommitAwareMetrics`(afterCommit 이연) ② 그 `afterCommit` 예외가 호출자에게 전파돼 **Kafka listener 가 이미 커밋된 이벤트를 재처리** → try/catch 격리 ③ `0 * metric` 우회(라벨 검사는 통과하고 alert 는 영원히 미발화) → 식 정본 고정, 그런데 신규 2종만 고정해 **기존 4종에 같은 우회 잔존** → 전체로 확장 (see ADR-0019). **false-green 3건 자백**: 타임아웃 테스트가 실제 잡 대신 메트릭 객체를 직접 호출 / `CommitAwareMetrics` 자체 테스트가 없어 즉시증가로 되돌려도 통과 / DLQ 계약이 order 에만 고정. 792 테스트·lint 11종·promql self-test 10종 그린. **미충족**: alert **발화** 미검증(ADR-0015 범위) · ④ 종결은 ④-d-2 · **④-d-2 는 계획 리뷰 3라운드(13→11→12건, 전량 반영) 후 사용자 승인으로 2a/2b 로 분할** — 규모(P1~P20)와 “실제 실행 없이 완료 선언 불가” 때문 · **④-d-2a ✅ [#92](https://github.com/Kimgyuilli/PeekCart/pull/92)** E2E 하네스 — `toss.payments.base-url` 설정화(운영/로컬/E2E 로 값이 갈리는 연결 정보라 프로파일 소유, base 는 도달 불가 sentinel·k8s 는 기본값 없이 강제) · PG stub(**paymentKey 접두사 불변 script** — 전역 모드는 시나리오 오염, `confirm`/`cancel`/`GET` 3종·호출 원장) · `docker-compose.e2e.yml`(`internal:true` 단일 부착·앱4·순차 기동, 정적 격리 lint) · cold-start 판정(**warm datadir 은 initdb 를 재실행하지 않아** `flyway_schema_history` 만으로 warm reuse 를 못 가린다 → volume 사전 검사+`run_id` marker) · **구독 정본 분리**(업무 21쌍은 `DlqTopology` 유도·복제 금지, DLQ intake4+quarantine3 만 신설, 상수↔`PeekcartService` 정본 정합 테스트) · **시나리오 4종 실제 스택 통과**(A 결제실패·B 예약실패·C 환불체인·D DLQ intake). **설계 2회 뒤집힘**: seed runner 는 “운영 bootJar 밖 source set” 이면 컨테이너에서 실행 불가·main 이면 인증 우회 스위치라 **양쪽이 막혀 있었다** → `DUAL_ACCEPT` 로 실제 컨트롤러 호출(새 운영 코드 0) / 계획이 요구한 `dispatch-enabled`·`reconcile-enabled` 는 stub+internal network 설계로 **아무도 쓰지 않게 되어 폐기**(환경변수 하나로 운영 스케줄러를 조용히 없애는 위험만 남음, ADR-0018 필수 구성요소). **diff 리뷰 3라운드 19건 전량 반영** — 2R 5건 중 3건이 1R 수정이 남긴 false-green, **3R 의 P1 1건은 3R 수정 자신이 만든 것**(환경 격리를 `SystemEnvironmentPropertySource` 로 복원하자 완화 매핑으로 `TOSS_PAYMENTS_BASE_URL` 재유입 — 하필 E2E compose 가 쓰는 이름). **실행이 잡은 11건은 전부 하네스 오류**이고 운영 코드 결함 0 — 그중 `TOSS_BASE_URL` 이 프로파일 리터럴을 못 이겨 dispatcher 가 실제로 `api.tosspayments.com` 을 호출했고 **`internal:true` 가 `UnknownHostException` 으로 막았다**(네트워크 차단의 라이브 증거). 813 테스트·lint 13종 그린. **미충족**: 수렴 미달(3R P1 1건·상한 종료, 3R 수정은 제3자 미검토) · 4종 **연속** 실행 불안정(각각은 통과, 근본 대응은 P10/P19) · 환불 회신 3소비자 종결·결과 3종 분기·stub ledger 순서·DLQ `attempt_count=2` · gateway `SIGNED_ONLY` 미검증 · **④ 미종결** · **④-d-2b 🔲 대기**(P10 시나리오 격리 · P11~P15 계약 매트릭스/matrix lint/JVM 증적 키/manifest 1:1 대조/파서 self-test · P16 음성 대조군 CI 상시 · P17 스케줄러 배선 계약 · P18 CI 배선 · P19 실행 예산 · P20 문서 동기화 + **④ 종결**. 착수 시 #92 diff 포함 재리뷰 선행) · **④-d-2b ✅ [#93](https://github.com/Kimgyuilli/PeekCart/pull/93)** ④ 종결 — **선행 재리뷰(#92 diff 3137줄) 5건(P0/P1 0)**: 3R 수정 4건은 전부 유효 판정으로 착수 조건 해소, 잔여 4건 반영(격리 lint 가 `services` 비어있음만 봐 **서비스를 지운 fixture 가 통과**·시나리오 C 직렬 발행·Flyway 개수만 비교·`ce7d75a` 이후 문서 3곳의 fail-fast 오안내) + **1건은 계획서 정정**(stub `confirm` 500 은 P6 의 실제 HTTP 진입점 요구와 모순, 조용한 통과 차단은 `STUB_UNSCRIPTED_KEY` 가 키 단위로 담당) · **P10** 시나리오 격리 — `processed_events` 를 `consumer_group` 만으로 조회해 **앞 시나리오 행이 만족시키던 false-green** 을 `eventId` 키잉으로, 시나리오 D 는 A 가 같은 토픽에 실제 `payment.failed` 를 흘리므로 poison 본문 marker 로 키잉(`DLT_ORIGINAL_KEY` 부재) + 실행 순서 계약(정본 1곳·스택 기동 전 검사, 잔여 시나리오는 꼬리 고정 — "배경 스케줄러 간섭 0" 은 `UNRESOLVED` 가 reconciliation 후보라 **단언하지 않는다**) · **P11~P15** 계약 매트릭스 26행 + 3분기 lint(**required-ID 정본을 lint 안에** — 매트릭스가 유일 입력이면 행 삭제가 검사 대상만 줄인다) + 증적 키 `testcase@classname`+`[SAGA-xxx]`(`testsuite@name` 은 `@DisplayName` 으로 덮여 키 불가) · 기존 19개 태깅 + **Notification `UNRESOLVED` 신설**(3×3 중 유일 부재) + manifest `evidence[key]={actual:…}`/`diagnostics` 분리 · **self-test 36종** · **P17** 스케줄러 배선 — 기존 테스트가 `@InjectMocks` 직접 호출이라 `@Scheduled` 를 지워도 통과했다 → 주기·lock 을 base 소유 타입 안전 properties 로(인라인 기본값 금지) + **실제 발화로 DB 가 바뀌는 것**을 보는 통합테스트, **변이 검사로 실증**(`@Scheduled` 제거 시 4건 전부 FAILED). 계획 원안의 "컨텍스트 기동 전 seed initializer" 는 **Flyway 가 기동 중 스키마를 만들어 성립 불가** → 주기만 짧게 덮고 운영 기본값은 별도 계약 테스트가 고정 · **P16** 음성 대조군 7종 실제 스택 통과 — **양성 대조군이 설계 결함 3건을 연속 반증**(주소 유도 실패 / 앱 이미지에 `python3` 부재라 음성이 **도구 없음으로 통과**하던 false-green / Docker Desktop 에서 브리지 게이트웨이가 VM 안 주소라 표적을 컨테이너로 이동), 판정은 **rc 특정**(127 을 격리로 오인 금지) · **P18** CI e2e 잡(PR 에서도 saga 4개 업로드·artifact 개수 검증·`if: always()` 증적) · **P19** 구간별 예산·`durations.tsv`, 그리고 **#92 미충족 #2(4종 연속 실행 불안정)의 근본 원인 해소** — 가설 3개(디버그 로깅·파티션 late discovery·이벤트 유실)가 실측으로 반증된 뒤, 실제 원인은 **`while read` 루프 안의 `docker compose exec -T` 가 루프 stdin(토픽 목록)을 삼켜** 20종 중 1종만 사전 생성되고 **같은 버그로 검증 루프도 1건만 돌아 "대조 실패 0" 이 vacuous** 였던 것. `</dev/null` 후 `scenario:b` **162~189초(실패/경계) → 21s/10s/14s**, **4종 3회 연속 통과**. 재발 방지로 `생성 성공 == 선언 개수` 계약화 · 토픽 파티션 정본은 `TopicBuilder` 에서 `--emit-topics` 로 유도 · **④-c-2b-4a ✅ [#105](https://github.com/Kimgyuilli/PeekCart/pull/105)** replay **개시 진입점** — **2b-4 를 4a(진입점 축)/4b(drain·롤백 축)로 분할**(계획 리뷰 3R 의 P0 5건 중 **4건이 drain 축**이고 앵커 컬럼이 `replay_deadline`→`outbox_events.created_at`→`last_replay_settled_at` 으로 **세 번 갈아엎혔다**. 진입점 축은 1R 이후 P0 0건) · **착수 전 코드 검증이 초안 전제 5건을 반증**(`kafkaAdmin.createAdminClient()` 미존재 API · C-31 의 "전 경로 root 잠금" 은 reconciler 가 예외 · **backfill 은 DML 이라 최종 스키마 parity 가 못 본다** → DML parity 신설 · `outbox_event_id` 소유가 표(target)/절차(root)에서 모순) · P18 reader(**byte[] + strict UTF-8 왕복**, compact·retention **실측** 거부, offset 일치로 compaction hole 방어) · P19 6 금지축 **독립 평가**+안전창 clamp+**`(ledgerOwner, topic)` 21 소비쌍 default-deny**(`stock.reservation.result` 를 order·payment 가 함께 소비해 토픽 단일 키로는 payment 구현 불가 — DB-per-service) + 도메인 aggregate 잠금으로 TOCTOU 차단 · P20 fence · P21 진입점(**ADMIN 전용**·조건부 claim·root 앵커·**I-1 을 잠금 안 2단계 검사로**(acknowledge 면제)·reconciler 스캐너/워커 분리) · P23 `V11/V9/V9/V7` · P25 lint 2종. **kill-switch 기본값 `false`** 라 배포해도 열리지 않는다 — 4b 가 선 뒤 연다. **diff 리뷰 2R(1R 8건 전량 · 2R 7건 중 6건)** — 1R 이 `StringDeserializer` 손실 변환으로 **fence 의 byte 동일성 주장이 거짓**이던 것과 권한 검사 부재를 잡았고, 2R 이 감사 주체가 **로그에만** 남던 것과 V-35 가 **관통이 아니던 것**을 잡았다. **변이 8종 red**(잠금 선행 제거가 red 인 것이 I-1 단순화의 근거) · parity self-test 26종 · **전 8모듈 1130 tests 0 실패**. **미충족**: 계획 미수렴(3R 상한·P0 3라운드 연속 → 4b 착수 전 4R 필요) · V-35 의 업무 consumer 실패는 시뮬레이션 · **§10 R9 진입점 운영 도달 경로 부재**(게이트웨이 미라우팅 — ④-c-2a 가 만든 기존 성질, ADR-0022 로 이관) · **④-c-2b-4b ✅ [#106](https://github.com/Kimgyuilli/PeakCart/pull/106)** ④ 종결 — **계획 4R·diff 리뷰 모두 사용자 지시로 미실행(수렴 미판정)**. 착수 전 코드 검증 C-44~C-55 로 대체해 미결 2건을 닫았다(ⓓ 상한 소유처 · §10 R9) · **P26 ADR-0022**(결정 6·기각 7, ADR-0020 §D6-1/§D6-2b/§D6-4 부분 무효화, Proposed→Accepted 2커밋) · **P24** `PUBLISH_UNKNOWN` 단방향 해제(**outbox 행 부재일 때만** — 남아 있으면 reconciler 와 경쟁하는 두 번째 종착 경로가 된다, 계획에 없던 좁힘) + 감사 2컬럼(**C-46 '마이그레이션 0개' 가 구현 중 반증** → V12/V10/V10/V8) · `replay-drain-preflight.sh`(**수집/판정 분리** — 판정만 fixture 로 관통 가능, self-test 11종) + `deploy-overlay.sh` 래퍼 + ConfigMap kill-switch 키 4개 + runbook §6 재작성·**§6-R 롤백 절차** · **P27** 게이트웨이 관리자 라우트 8종(**§10 R9 처분** — 진입점은 있었으나 인증 주체를 세울 수 없어 **도달 불가**였다. 권한 판정은 서비스에 남기고 `{id:[0-9]+}` 로 actuator 누출을 매칭 단계에서 차단) · **P22 표본 0 → '미측정'**(fixture 대체·0%표기 거부) · **false-green 1건 자백**(라우트 테스트가 `AsyncPredicate` 를 Boolean 과 비교해 **누출 음성 6종이 전부 vacuous-green**) · **회귀 1건**(라우트 수 9 고정 → 17) · 변이 14종 red · **전 8모듈 1130→1153 tests 0 실패**. **미충족**: 리뷰 수렴 미판정(완료 조건 2 미달) · V-40 실제 스택 관통과 preflight **수집 계층** 미검증(클러스터 부재) · kill-switch 실제 개방은 운영 작업(§9 4단계·R2) | A3 | L-013 (✅ 해소, #84) | ✅ |
| ⑤ | CQRS 로컬 캐시 (착수 전 코드 검증에서 **범위 재확정** — 로드맵이 ⑤ 에 귀속시킨 4표면 중 **3개는 구현 ① strangler-2([#57])/strangler-4([#61])에서 이미 완료**돼 있었다: `product.updated` 발행(`ProductOutboxEventPublisher`, 7필수필드+version)·Order 로컬 캐시 구독/갱신(`ProductPriceCacheConsumer`+`product_price_cache`, 멱등 `processed_events`·stale-skip `source_version`)·`ProductPort` 동기 결합 제거. 코드 주석이 이미 `"CQRS ⑤, strangler-2"` 로 자기 귀속을 적어둔 상태였다. **실질 잔여는 L-006 1건** — `CacheErrorHandler` 구현체 0건 확인 · **P1~P3** `CacheConfig implements CachingConfigurer` + `ResilientCacheErrorHandler`(get/put 삼킴, evict/clear 는 WARN+메트릭 — evict 실패는 DB 커밋 후 TTL 만료까지 stale 창을 연다) + `cache_fallback_total{cache,operation}` · **P4** Redis 타임아웃 유계화 500ms/300ms — **1s 는 산술적으로 불가**했다(`@Cacheable` 1요청이 get 실패→DB→put 실패로 timeout 을 2회 소비) + `spring.data.redis` 하위 트리를 base(timeout)/프로파일(host·port)이 나눠 갖는 구성을 ADR-0007 조건1 추정 대신 **바인딩 회귀 테스트로 실증** · **P5/P6** Toxiproxy 장애 주입 — 컨테이너 정지는 **연결 거부만** 재현하고 되돌릴 수 없어 순서 의존을 만든다 → `Proxy#disable()`(거부)와 downstream `timeout(_,0)` toxic(무응답)을 **분리**, backend Redis 의 `@ServiceConnection` 을 **제거**하고 `@DynamicPropertySource` 로 프록시에 배선(유지하면 앱이 프록시를 우회해 전 시나리오 false-green) + V0 경유 단언 · **put 실패는 무해하지 않다** — 캐시가 안 채워져 후속 요청이 전부 DB 를 친다(스파이로 findById N회 실증, 양성 대조군은 1회) · **리뷰가 내 변경의 실제 버그를 잡았다**: `@Configuration` 에 `MeterRegistry` 를 직접 주입해 `MeterRegistryCustomizer` 적용 전에 레지스트리가 생성되면서 `application=product-service` 태그(ADR-0009 S2)가 사라졌다 → `ObjectProvider` 지연 해석 · **변이 검사로 실증** — `errorHandler()` 오버라이드를 맨 `@Bean CacheErrorHandler` 로 바꾸면 7건 중 6건 FAILED(정상 경로인 양성 대조군만 통과), Spring 이 `CachingConfigurer` 에서만 핸들러를 수집한다는 바이트코드 전제가 런타임에서 확인됨 · **P7** runbook(복구=TTL 만료 대기, 즉시 해소는 SCAN+UNLINK — `KEYS` 금지, 배포는 타임아웃+핸들러 원자 배포, 감시 임계 PromQL 고정) · **P8/P11** `04 §9-13`·`05:290` 정정 + **ADR-0012 Update Log**(`fix(adr):`) · **P10** `hpx_plan_lint` 가 zsh 에서 죽던 `local path`→`PATH` 오염 수정 · **diff 리뷰 5라운드** — 1R→4R 은 매 라운드가 직전 라운드 수정이 만든 결함을 잡았다(같은 축에서의 과보정 왕복), 5R 에서 예외 분류 축 0건으로 수렴 | A3 | L-006 (✅ 해소) | ✅ [#94](https://github.com/Kimgyuilli/PeakCart/pull/94) |
| ⑥ | Cursor 페이지네이션 (**착수 전 코드 검증에서 범위 재정의** — offset 성능 문제로 잡았으나 `OrderController:52` 의 `@PageableDefault` 에 `sort` 가 없어 **정렬 자체가 미지정**이었다. offset 페이징으로서의 결과 정합성도 없던 상태 → 1차 가치는 성능이 아니라 **결정적 순서 계약 확립**. 계획서 `task-impl6-cursor-pagination.md` P1~P14) — 정렬 키 `(ordered_at DESC, id DESC)`·불투명 base64url 커서(고정 6자리 μs + `ResolverStyle.STRICT`[기본 SMART 는 `2026-02-30`→`02-28` 보정] + `DATETIME(6)` 저장범위 불변식)·`V7 idx_orders_user_id_ordered_at (user_id, ordered_at)` [**`id` 미명시 — InnoDB PK 암묵 부착을 실측 확인: `used_key_parts=[user_id, ordered_at, id]`**]·`size+1` 로 `hasNext` 판정(**COUNT 제거**)·입력 파싱을 프레젠테이션(`OrderPageQuery`)이 소유[서비스에 두면 `@WebMvcTest` 가 검증을 실행조차 못 함]·`size` 를 `String` 수신[`int` 면 `size=abc` 가 **500**]·폐기 `page`/`sort`/`offset` 은 **400 `ORD-012`**[조용한 무시는 구 클라이언트가 항상 1페이지만 받는 오동작을 감춘다]. **BREAKING**: `totalElements`/`totalPages` 제거. **계획 리뷰 5R(12→10→8→6→6, 매 라운드 '직전 수정이 만든 새 결함' 5→5→3→5 — 미수렴 종료) + diff 리뷰 2R(1R 5건 전부 false-green → 2R **P1=0 수렴**)**. **4R 이 채택한 `DATETIME(6)` 상한 `.499999` 가 5R 실측에서 오류로 판명**(8.0.46 에 직접 INSERT: `.499999`/`.500000`/`.999999` 전부 저장 — 그대로 갔으면 정상 데이터를 400 으로 거부). 실측 `cursor [20,20,20]` vs `offset [40,420,920]`, 증적 `evidence/impl6-cursor-queryplan-20260830.md`. **미충족**: 전 모듈 스위트 로컬 미완주(`ContainerLaunchException` — 자원, 코드 아님) → **CI 확인 후 머지** · `GlobalExceptionHandler` 타입변환 핸들러 부재(선재 갭, 5서비스 영향이라 우회) · 상품 목록은 offset 유지(⑤ 캐시 표면 결합) · 벽시계 미측정 · MySQL 이미지 핀은 실행계획 테스트만 | — | — | ✅ [#96](https://github.com/Kimgyuilli/PeakCart/pull/96) |
| — | User 토큰 회전 §1.1/§3.1 축 실증 (로컬 드릴) — runbook §6 미검증분 회수. **G1**(401 구간 실재) 결정적 인과쌍으로 확증(로그인·JWKS 경로를 각각 Pod 에 고정: JWKS→구 Pod 10/10 **401** ↔ JWKS→신 Pod 10/10 **200**, 토큰 kid 동일) · **G2**(§3.1 gate 유효성) 고정 skew 상태에서 차단 확인 · 정상 순서 ①~⑤ 완주(③ 롤아웃 전 구간 **401=0**). **runbook §1.1 정정** — "어느 Pod 가 응답할지 모른다"가 아니라 gateway 커넥션 풀링으로 **한 Pod 에 고정**된다(경유 로그인 121/121 동일 Pod ↔ 새 커넥션 직접 호출 7/20 분배). cooldown 재시도로 자가치유 안 되고, 장애가 gateway 인스턴스별 부분 401 로 나타난다. 하네스 `k8s/overlays/minikube-rotation-drill`(드릴 전용·승격 금지) | ADR-0013 D1 | — | ✅ 로컬 드릴 [#116](https://github.com/Kimgyuilli/PeakCart/pull/116) |
| — | D-002 격리 재측정 — **하네스 선행 ✅ [#119](https://github.com/Kimgyuilli/PeakCart/pull/119)** (`gke-d002bc` overlay·시드 SQL·k6 3종. 실측 P4~P13 은 GKE 기동 필요 → 별도 세션). 착수 전 코드 검증에서 **전제 6건 반증** — V7 order/payment 는 `SIGNED_ONLY` 라 평문 `X-User-*` 무시 → gateway 우회 불가 · V8 RateLimiter 는 **사용자별** 키(`userKeyResolver`)라 N×40rps 로 우회, 설정 미변경 · **V10 D-002a 가 배제한 CPU 는 product-service 의 것** — `gke-d002a` 가 infra 를 patch 하지 않아 MySQL 은 내내 `limits.cpu:500m` 이었고 그 축은 미변경 → P5 1순위가 InnoDB 내부에서 **MySQL CPU 상한**으로 이동 · V11 상품 SQL 시드는 `product.updated` 미발행 → `product_price_cache` 공백 → 주문 전멸(`ORD-007`), admin API 경유로 교체 | — | D-002 (분리 후) | 🔄 추적 (하네스 완료 · 실측 미수행) |
| — | CI `build` job 분해 (lint / test 6-shard / guards / gate) — 구현 ⑥(#96)의 `build` **32m23s** 직렬 실행이 동기. **레포 분리를 검토하고 기각**(비용 동인이 구조가 아니라 컨테이너 165회 기동 × 병렬성 0 — 나눠도 기동 수는 그대로이고 공유 모듈 강결합·크로스서비스 lint·단일 스택 e2e 가 비용만 늘린다). `:module:test` 가 아니라 **`:module:build`**(실측 154==154 — `test` 만 돌리면 `bootJar`·`testFixturesJar` 검증 소실) · **`guards` job 분리**(가드 5종이 루트 `check` 에 걸려 있어 매트릭스로만 나누면 조용히 사라진다) · **`merge-multiple` 폐기**(last-writer-wins 로 증적이 덮인다 → artifact 별 수신 후 충돌 검출 병합) · **`run_attempt` 미사용**("Re-run failed jobs" 는 실패 job 만 재실행) · **선행 실패 전파**(안 하면 테스트 red 인데 gate green → images/e2e/publish 진행). `ci-test-matrix-lint` 신설(self-test 22종, 가드 목록은 `build.gradle` check dependsOn 에서 read). 계획 3R + diff 3R — **3R 에서 manifest·rc 기구가 결함 생산원임을 확인해 통째로 폐기**(지적 9건 중 4건이 그 기구에서만). **실측**: 전체 run **51m44s → 30m23s(−41%)**, 테스트 단계 **33m20s → 11m23s(−66%)**, 합산 runner-minutes ×1.18(중복 컴파일). shard 소요가 `@Container` 선언 수와 단조 증가 — D2 분할 근거 확인. **서비스 shard 비 3.39(>2.0)이나 재분할 안 함** — 임계 경로를 order(11m22s)가 아니라 **e2e(15m28s)가 쥐고 있어** 쪼개도 전체가 안 줄어든다(증적 `evidence/ci-test-matrix-20260901.md`). **미충족**: T2·T3b·T6·T9(전부 실패 경로라 전량 통과 run 에서 미검증) · `test-reports` 세 트리 내용 미확인 · diff 리뷰 수렴 미달(상한) · 컨테이너 재사용/병렬 설정/path filter/테스트 태깅은 범위 밖 | — | — | ✅ [#97](https://github.com/Kimgyuilli/PeakCart/pull/97) |

---

## 진입 전 부채 해소 (버킷 1) — ✅ 완료 (아카이브)

> 9개 PR 전부 완료(Tier A · D-012~D-015 · D-017~D-019, PR #37~#43). 상세 PR 테이블·시퀀스는 `docs/progress/phase4-prep-debt-roadmap.md §2`(SSOT)에 보존.

---

## 개발 부채 (Tech Debt)

> 해결 완료(D-001~D-012) 상세는 아카이브(`TASKS-archive-phase1-3.md §개발 부채`) 보존. 여기서는 **live + 신규**만 추적.

### Live / 신규

| ID | 영역 | 요약 | 묶음 | 상태 |
|---|---|---|---|---|
| D-002 | Performance | 캐시 TPS ×2.31(목표 ×3 미달). 1차 병목 CPU 확증, 2차 후보(MySQL 풀 / Redis 락 contention) 미분리. **착수 전 코드 검증에서 범위 재정의(2026-09-16)** — 원문이 서로 다른 두 측정을 묶고 있었고(D-002a 캐시 배속=product 읽기 / D-002b p95 30.21s=주문 생성), **D-002b 의 전제가 소멸**했다: 주문 생성이 더는 재고를 차감하지 않는다(`OrderCommandService.createOrder` = order insert + outbox publish). 분산 락은 product-service `InventoryLockFacade` 로 이동했고 **Kafka consumer 가 부른다** → 동기 경로의 락 대기 timeout 은 그 형태로 재현 불가. 재정의: **D-002a**(캐시 배속 격리 — 토글 `peekcart.cache.enabled` 로 대조군) · **D-002b'**(주문 생성 신규 기준선 — Hikari 설정이 레포에 0건이라 **기본 풀 10** 이 1순위 변수) · **D-002c**(비동기 예약 경로 락 경합). 실측은 GKE 과금 세션 필요(쿼터 12 vCPU · co-location 경합 · RateLimiter 40 req/s 제약). 계획서 `docs/plans/task-d002-rescope.md` **D-002a 실측 완료(2026-09-16)** — `×3` 목표가 조건 없는 숫자였다. 배속은 CPU 여유·엔드포인트 구조의 함수로 **×1.23~×2.39** 에서 움직인다. 1차의 **×2.31 은 이상치가 아니라** CPU 포화 조건의 정상값(같은 조건에서 list ×2.39 재현). 가장 큰 레버는 캐시가 아니라 **`limits.cpu: 1000m`**(캐시 ON 유지·limit 만 3000m → list 325→715 rps). detail 은 캐시 적중해도 재고를 DB 에서 읽어(`ProductQueryService.getProduct`) 이득이 절반(×1.23 vs ×2.02). **2차 병목은 CPU 도 Hikari 풀 10 도 아니다** — 풀 50 으로 올려도 개선 없음(재정의 문서의 1순위 가설 반증), MySQL 내부가 남았고 **미분리**. 증적 `evidence/d002a-gke-20260916-0030.md` · [#117](https://github.com/Kimgyuilli/PeakCart/pull/117). **b'/c 하네스 선행 [#119](https://github.com/Kimgyuilli/PeakCart/pull/119)** — 착수 전 코드 검증에서 `gke-d002a` 가 infra 를 patch 하지 않았음이 드러나 **MySQL 은 내내 `limits.cpu:500m`** 이었고 그 축은 한 번도 변경되지 않았다. 즉 "2차 병목은 CPU 도 풀도 아니다" 의 CPU 는 **product-service 의 CPU** 였고, **MySQL 자신의 CPU 상한이 미분리 상태로 "MySQL 내부" 안에 들어가 있었다** — 다음 세션 P5 의 1순위 | Phase 4 Order Service 분리 후 격리 재측정(게이트 충족) | 🔄 추적 (D-002a 완료 · b'/c + 천장 원인 미분리) |
| D-012 | CI / Deliverable | CI 가 품질 게이트가 아니다 — PR Docker build·smoke 부재, branch protection 미설정, NS 누출 lint 부재 | 버킷 1 (L-014/015/017) | ✅ 완료 |
| D-013 | Resilience | 발행 경로 resilience 갭 — DLQ 발행 미확정(유실), outbox `.get()` 타임아웃 부재와 polling cycle 상한 미정의(워커 잠식) | 버킷 1 (L-010/012) | ✅ 완료 |
| D-014 | Observability | 선결 측정 표면 부재 — 캐시 적중률 / outbox 파이프라인 메트릭 | 버킷 1 선택 (L-005/009) | ✅ 완료 |
| D-015 | Deploy/CI | CI push image repo(`peekcart`) ↔ K8s base/GKE 참조(`peakcart`) 계약 불일치 → GHCR→AR 복사·base 배포 실패 가능 | 버킷 1 (도식검토) | ✅ 완료 |
| D-017 | Observability | Grafana alert rule 존재하나 Slack contact point/provisioning 부재 → "alert→Slack" 경로 미완성. 범위 정정(②)으로 봉합, delivery 는 L-004 이관 | 버킷 1 (도식검토) | ✅ 완료 |
| D-018 | Docs | `loadtest/reports/2026-04-29/REPORT.md` Redis PVC 1Gi ↔ 현 매니페스트 512Mi 드리프트 | 버킷 1 (도식검토) | ✅ 완료 |
| D-020 | Resilience / Payment | **결제 승인 ↔ 로컬 커밋 불일치** — `PaymentCommandService` 가 DB 트랜잭션 안에서 Toss 승인을 호출하고 그 뒤 상태·Outbox 를 저장한다. 승인 성공 후 커밋이 실패하면 **외부 과금은 남고 로컬은 롤백**된다. `WebhookService` 는 웹훅을 로그로만 적재하고 상태를 복구하지 않는다. 필요한 것은 승인 idempotency key + 웹훅/조회 기반 reconciliation + 영속 상태머신 (구현 ④ GW-1 리뷰 #3 에서 발견, choreography saga 가 아닌 **PG reconciliation** 표면이라 ④ 범위 밖) → **해소 ([#107](https://github.com/Kimgyuilli/PeakCart/pull/107), ADR-0023)** — 승인을 T1(claim·실제 `paymentKey` 커밋) / PG 호출(트랜잭션 밖·안정 멱등키 `approve-{orderId}`) / T2(확정) 로 분해 + `payment_approvals` 원장(fence·generation·CLAIMED/SUCCEEDED/FAILED/UNRESOLVED) + 조회 기반 `ApprovalReconciliationScheduler`. **핵심은 롤백이 `paymentKey` 까지 되돌려 사후 확인 수단이 사라지던 것**(계획 C-3 확대). `confirm` 을 `.retrieve()`→`.exchange()` 분류로 바꿔 타임아웃이 영구 실패로 확정되던 경로 제거 · 고아 과금(PG 성립 ↔ 로컬 CANCELLED/FAILED)은 `requestRefundForOrphanedCharge` 로 환불 라우팅(감지 4지점) · 웹훅은 nudge 만(진실의 출처 아님) · `PAY-012`(결과 불명, 409)/`PAY-013` 신설. **미충족**: 실 PG 장애 주입 미검증(진입 조건 그대로) · Codex 리뷰 미호출(사용자 지시)이라 수렴 판정 없음 · T1 커밋 지연 벽시계 미측정 | Phase 4 후속 (외부 PG 장애 주입 환경 필요) | ✅ |
| D-019 | Testing | `OutboxKafkaIntegrationTest.orderCancelled_e2e` CI 간헐 실패 → **(a) 타이밍 flake 확정**(프로덕션 회귀 아님). D-013 producer 타임아웃 타이트화로 콜드 스타트 첫 발행이 실패하면 단발 poll 테스트는 재폴링이 없어 PENDING 고착 → `await` 타임아웃. 프로덕션은 스케줄러 재발행으로 자가치유. 테스트만 `pollUntilPublished` 로 수정(하드닝 유지) | 버킷 1 마무리분 (D-013 여파) | ✅ 완료 |

### 해결 완료 (아카이브 참조)

D-001(✅), D-005(✅), D-006(✅), D-007(✅), D-008(✅), D-009(✅), D-010(✅), D-011(✅), D-012(✅) · D-003(Won't Fix) · D-004(운영지식) — 상세: `docs/progress/TASKS-archive-phase1-3.md §개발 부채`.

---

## Phase 4 — MSA 분리 (예정)

> 로드맵 §3(버킷 2) 이관 부채를 각 Phase 4 task 에 편입. 착수 시 D- 승격 또는 task 항목 흡수.

| 순서 | 작업 | 편입 부채 |
|---|---|---|
| 1 | Gradle 멀티모듈 전환 | L-016(a) digest 고정, L-020(2) consumer group 라벨, D-002 격리 재측정 |
| 2 | 서비스별 DB 분리 | L-008/L-011 retention(보존기간=멱등성 창 상한 결정) |
| 3 | Spring Cloud Gateway | **보안 묶음** L-001/L-002/L-003/L-019 (RS256 전환 + KMS/Vault + Reuse Detection + 인증 관측성) |
| 4 | Choreography Saga | — |
| 5 | CQRS 로컬 캐시 | L-006 Redis fallback (L-005 선결) |
| 6 | Cursor 페이지네이션 | — |
| — | 운영 관측성 | L-004 Slack 채널 재설계 |

상세: `docs/07-roadmap-portfolio.md §16` · 로드맵 §3.

---

## 보류 (측정 후 결정)

> 게이트: **17편 후속 부하 세션** 실측. 나오면 모놀리스 단계 선제 승격, 아니면 Phase 4 분리 시 자연해소(L-007)/필수화(L-013).

| ID | 영역 | 측정 게이트 |
|---|---|---|
| L-007 | 주문 *생성* 경로 "락 ⊃ 트랜잭션" 불변식 + 재고 차감 retry 정책 미정 | 동일-상품 경합 시 재고 차감 `PRD-004`/`OptimisticLockingFailureException` 응답률 유의 |
| ~~L-013~~ | ~~주문 *상태 전이* 동시성(`Order @Version` 부재)~~ | **✅ 해소 (2026-08-13, [#84](https://github.com/Kimgyuilli/PeakCart/pull/84))** — 게이트대로 실측 선행. `payment.completed` 소비 ↔ 타임아웃 취소를 두 EntityManager 가 같은 스냅샷을 읽도록 강제한 **결정적** 재현으로 양방향 lost update 확증(취소 선커밋→`PAYMENT_COMPLETED`, 결제 선커밋→`CANCELLED`) → `@Version` 승격 + 충돌 정책 확정. 상세: 계획서 §5.1 |

상세·승격 시 동반 결정: 로드맵 §4.
